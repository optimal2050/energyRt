---
title: "`r params$title`"
report-class: calendar
output:
  html_document:
    self_contained: true
    md_extensions: "-native_divs-native_spans"
  pdf_document:
    latex_engine: xelatex
  word_document: default
params:
  title:      "Calendar"
  name:       ""
  desc:       ""
  class:      "calendar"
  image_file: NULL
  logos:      NULL
  info_df:    NULL
  main_plot:  NULL
  frames_df:  NULL
  share_df:   NULL
header-includes:
  - \usepackage{booktabs}
---

```{r en-setup, include=FALSE}
library(ggplot2)

fmt <- energyRt::report_setup()
tbl <- function(df, caption = NULL, digits = 4L, ...)
  energyRt::report_tbl(df, caption, digits, output = fmt, ...)
emit_img <- function(path, frac = 1)
  energyRt::report_img(path, frac, output = fmt)
```

```{r en-css, results='asis'}
energyRt::report_css(fmt)
```

```{r en-logo, results='asis'}
if (length(params$logos) > 0) {
  energyRt::report_img_row(params$logos, frac_total = 0.9, output = fmt)
}
```

```{r en-header, results='asis'}
energyRt::report_header(params$name, params$desc,
                        image_file = params$image_file, output = fmt)
```

```{r en-info, results='asis'}
energyRt::report_sec("Key facts", output = fmt)
tbl(params$info_df)
```

```{r en-hero, results='asis', eval=!is.null(params$main_plot)}
emit_img(energyRt::report_plot_png(params$main_plot, 7.5, 3.8), 1)
```

```{r en-frames, results='asis', eval=!is.null(params$frames_df)}
tbl(params$frames_df, caption = "Timeslices per timeframe")
```

```{r en-share, results='asis', eval=!is.null(params$share_df)}
tbl(params$share_df, caption = "Timeslice shares (first rows)",
    max_rows = 25)
```
