% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-trade.R
\docType{class}
\name{class-trade}
\alias{class-trade}
\title{An S4 class to represent inter-regional trade}
\description{
An S4 class to represent inter-regional trade
}
\details{
Trade objects are used to represent inter-regional exchange in the model.
Without trade, every region is isolated and can only use its own resources.
The class defines trade routes, efficiency, costs,
and other parameters related to the process. Number of routes per trade object is not
limited. One trade object can have a part or entire trade network of the model.
However, it has a distinct name and all the routs will be optimized together.
Create separate trade objects to optimize different parts of the trade network
(aka transmission lines).
}
\section{Slots}{

\describe{
\item{\code{name}}{character. Name of the trade object, used in sets.}

\item{\code{desc}}{character. Description of the trade object.}

\item{\code{commodity}}{character. The traded commodity short name.}

\item{\code{routes}}{data.frame. Source and destination regions. For bivariate trade define both directions in separate rows.
\describe{
\item{from}{character. Source region.}
\item{to}{character. Destination region.}
}}

\item{\code{trade}}{data.frame. Technical parameters of trade.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{slice}{character. Time slice to apply the parameter, NA for every slice.}
\item{trade}{numeric. Trade volume.}
}}

\item{\code{aux}}{data.frame. Auxiliary commodity of trade.
\describe{
\item{acomm}{character. Name of the auxiliary commodity (used in sets).}
\item{unit}{character. Unit of the auxiliary commodity.}
}}

\item{\code{aeff}}{data.frame. Auxiliary commodity efficiency parameters.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{acomm}{character. Name of the auxiliary commodity (used in sets).}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{slice}{character. Time slice to apply the parameter, NA for every slice.}
\item{trade2ainp}{numeric. Trade-to-auxiliary-input-commodity coefficient (multiplier).}
\item{trade2aout}{numeric. Trade-to-auxiliary-output-commodity coefficient (multiplier).}
}}

\item{\code{invcost}}{data.frame. Investment cost of the trade capacity (per unit of capacity).
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{invcost}{numeric. Investment cost.}
\item{wacc}{numeric. Weighted average cost of capital used to annuitise \code{invcost} for this corridor. Overrides the model-wide \code{wacc} (see the model \code{discount} argument). The social discount rate is never used here.}
\item{payback}{numeric. Cost-recovery period in years. Where given it replaces \code{olife} in the annuity AND in the years over which the annuity is charged, so the investment is repaid over \code{payback} years while the capacity keeps operating for its full operational life. Must be positive and not exceed \code{olife}. Unset (or 0) means recover over \code{olife}. Implemented for the GLPK solver only.}
\item{eac}{numeric. Equivalent annual cost, supplied directly instead of being computed from \code{invcost}, \code{wacc} and the lifetime. Where given it wins; where absent the annuity is computed. Mutually exclusive with \code{invcost} per row.}
\item{retcost}{numeric. Costs of early retirement of the trade capacity, default is 0.}
}}

\item{\code{fixom}}{data.frame. (not implemented!) Fixed operation and maintenance costs.}

\item{\code{varom}}{data.frame. (not implemented!) Variable operation and maintenance costs.}

\item{\code{capacity}}{data.frame. (not implemented!) Capacity parameters of the trade object.}

\item{\code{vintage}}{data.frame. Investment window and operational life of the trade object, one row per vintage. Replaces the former \code{start}, \code{end} and \code{olife} slots. A vintage is a separately investable variant that keeps the characteristics of its build year for its whole life, so several vintages of one corridor mean several capacities on the same \verb{(src, dst)} route, with their flows summed in the commodity balance. \code{region} and \code{cluster} are present for a uniform shape across process classes but are unused for trade: its scope comes from the route endpoints, and \code{routes} already provides the multiplicity a cluster dimension would add.
\describe{
\item{vintage}{character. Vintage label, normally the build year as a string. NA for an un-vintaged trade.}
\item{region}{character. Unused for trade (no \code{region} slot); present for consistency with the other classes.}
\item{cluster}{character. Unused for trade (no cluster dimension); present for consistency with the other classes.}
\item{start}{integer. The first year the trade object is available for investment. Defaults to the vintage year.}
\item{end}{integer. The last year the trade object is available for investment. Defaults to the vintage year.}
\item{olife}{integer. Operational life of the trade object in years.}
}}

\item{\code{cap2act}}{numeric. Capacity to activity ratio.}

\item{\code{optimizeRetirement}}{logical. Incidates if the retirement of the trade object should be optimized. Also requires the same parameter in the \code{model} or \code{scenario} class to be set to TRUE to be effective.}

\item{\code{misc}}{list. Additional information.}
}}

