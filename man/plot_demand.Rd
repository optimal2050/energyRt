% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_demand}
\alias{plot_demand}
\alias{autoplot.demand}
\title{Visualize a demand object}
\usage{
plot_demand(object, type = c("area", "line"), years = NULL, ...)

\method{autoplot}{demand}(object, type = c("area", "line"), years = NULL, ...)
}
\arguments{
\item{object}{A \code{demand} object.}

\item{type}{\code{"area"} (annual totals) or \code{"line"} (slice profiles).}

\item{years}{Optional integer vector of years. For \code{"area"} these are the
interpolation targets (default: range of the given years); for \code{"line"}
they filter which given years are shown.}

\item{...}{Passed to \code{\link[=getData]{getData()}} (e.g. \verb{region =} filter).}
}
\value{
A \code{ggplot} object (or \code{NULL}, invisibly, if there is nothing to plot).
}
\description{
Two views of a \code{demand} object:
\describe{
\item{\code{type = "area"} (default)}{\strong{aggregated} demand -- slice values are
summed to annual totals and drawn as an area over the years (stacked by
region), with the given data years marked as points.}
\item{\code{type = "line"}}{\strong{profiles} -- the within-year demand shape by
region and year. Slices with an hour tag (\code{"..._h07"}) are drawn against
the hour of day (faceted season x region when a season prefix is
present); other calendars fall back to the slice sequence.}
}
}
