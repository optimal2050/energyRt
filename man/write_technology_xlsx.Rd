% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlsx-technology.R
\name{write_technology_xlsx}
\alias{write_technology_xlsx}
\title{Write \code{technology} object(s) to an Excel workbook}
\usage{
write_technology_xlsx(
  x,
  path,
  include_empty = TRUE,
  drop_empty_cols = FALSE,
  header_fill = "#DDEBF7",
  info = TRUE,
  overwrite = TRUE
)
}
\arguments{
\item{x}{A \code{technology} object, a (named) list of \code{technology} objects, or a
\code{repository} (its \code{technology} members are written).}

\item{path}{Path to the output \code{.xlsx} file.}

\item{include_empty}{Logical; if \code{TRUE} (default) every data.frame slot is
written, including empty ones (header only) so the full class structure is
visible and the file can serve as an editable template. If \code{FALSE}, only
slots with at least one row are written.}

\item{drop_empty_cols}{Logical; if \code{TRUE}, columns that are entirely empty
(all \code{NA}) within a populated slot's mini-table are dropped, so only
columns that carry data are written. Empty slots (no rows) keep their full
header. Default \code{FALSE}. Round-trips safely: missing columns are restored
from the class prototype on read.}

\item{header_fill}{Header-row background colour (hex string, e.g.
\code{"#DDEBF7"}). Set to \code{NA} or \code{NULL} to disable the fill (headers stay
bold). Default \code{"#DDEBF7"} (light blue).}

\item{info}{Logical; if \code{TRUE} (default) a leading \code{Info} worksheet is added
documenting every slot and column of the \code{technology} class (names, types
and descriptions, taken from the class documentation). \code{\link[=read_technology_xlsx]{read_technology_xlsx()}}
ignores this sheet.}

\item{overwrite}{Logical; overwrite \code{path} if it exists (default \code{TRUE}).}
}
\value{
(Invisibly) \code{path}.
}
\description{
Serialises one or more \link{technology} objects to a \code{.xlsx} file, one technology
per worksheet. Each slot of the class is written as a labelled mini-table
whose header row matches the columns of the corresponding data.frame slot
(\code{names(slot(tech, s))}). Scalar slots (\code{name}, \code{desc}, \code{cap2act}, \code{region},
\code{timeframe}, \code{fullYear}, \code{optimizeRetirement}, \code{misc}) are written into a
leading \code{meta} block. The workbook can be read back with
\code{\link[=read_technology_xlsx]{read_technology_xlsx()}}.
}
\seealso{
\code{\link[=read_technology_xlsx]{read_technology_xlsx()}}

Other technology process:
\code{\link[=newTechnology]{newTechnology()}},
\code{\link[=read_technology_xlsx]{read_technology_xlsx()}}
}
\concept{technology process}
