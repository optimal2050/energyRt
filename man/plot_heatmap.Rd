% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Heatmap of timeslice-indexed values over a calendar}
\usage{
plot_heatmap(
  x,
  calendar = NULL,
  value = NULL,
  facet = NULL,
  palette = "D",
  name = NULL
)
}
\arguments{
\item{x}{A \code{data.frame} with a \code{slice} column and a numeric value column, a
named numeric vector (names are slices), or a \code{calendar} object (then the
slice \code{share} — or \code{value} column — is shown).}

\item{calendar}{A \code{calendar} object giving the layout (matched to \code{x} by
slice), or a format string (e.g. \code{"d365_h24"}). If \code{NULL}, the format is
guessed from the slice names with \code{\link[=tsl_guess_format]{tsl_guess_format()}}.}

\item{value}{Name of the value column in \code{x} (defaults to the single numeric
column, or \code{share} for a calendar).}

\item{facet}{Optional timeframe level(s) to facet by. \code{"month"} over a
day-of-year format splits the year into monthly panels (x = day-of-month).}

\item{palette}{Viridis color option for the fill scale.}

\item{name}{Legend title (defaults to the value name).}
}
\value{
A \code{ggplot} object.
}
\description{
Lays timeslice-indexed values on a 2-D grid whose axes follow the calendar
structure: the finest timeframe becomes the y-axis, the next-finest the
x-axis, and any coarser level(s) become facets — e.g. for a \code{d365_h24}
calendar, \code{x = day-of-year}, \code{y = hour-of-day}. Useful for load curves,
renewable profiles, prices, and other sub-annual series.
}
\examples{
\dontrun{
data("calendars", package = "energyRt")
cal <- calendars$d365_h24
prof <- data.frame(slice = cal@timetable$slice,
                   load  = runif(nrow(cal@timetable)))
plot_heatmap(prof, calendar = cal, value = "load")
plot_heatmap(prof, calendar = cal, value = "load", facet = "month")
}
}
