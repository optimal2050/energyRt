% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-storage.R
\docType{class}
\name{storage-class}
\alias{storage-class}
\title{An S4 class to represent storage type of technological process.}
\description{
Storage type of technological processes with accumulating capacity of a commodity.
}
\details{
Storage can be used in combination with other processes, such as
technologies, supply, or demand to represent complex technological chains,
demand or supply technologies with time-shift.
Operation of storage includes accumulation, storing, and release
of the stored commodity. The storing cycle operates on the ordered
time-slices of the commodity timeframe. The cycle is looped either
on an annual basis (last time-slice of a year follows the first time
slice of the same year) or within the parent time-frame (for example,
when commodity time-frame is "HOUR" and the parent time-frame is "DAY" then
the storage cycle will be a calendar day).
}
\section{Slots}{

\describe{
\item{\code{name}}{character. Name of the storage (used in sets).}

\item{\code{desc}}{character. Description of the storage.}

\item{\code{commodity}}{character. Name of the stored commodity.}

\item{\code{aux}}{data.frame. Auxiliary commodities.
\describe{
\item{acomm}{character. Name of the auxiliary commodity (used in sets).}
\item{unit}{character. Unit of the auxiliary commodity.}
}}

\item{\code{region}}{character. Region where the storage technology exists or can be installed.}

\item{\code{cluster}}{data.frame. Declaration of the storage clusters -- parallel sub-processes of the same storage with their own capacity, availability and costs. The motivating cases are site-constrained storage (pumped-hydro head classes, CAES caverns) and duration classes via a per-cluster \code{cap2stg}. Optional: when empty, cluster labels are harvested from the other slots; when populated it is authoritative and an undeclared label raises an error.
\describe{
\item{cluster}{character. Cluster label. Must match the \code{cluster} column values used in the other slots.}
\item{desc}{character. Human-readable description of the cluster, used in reports.}
\item{region}{character. Region the cluster exists in, NA for every region.}
\item{order}{integer. Optional display/ranking order; controls the order variants are created and reported in.}
}}

\item{\code{vintage}}{data.frame. Investment window and operational life of the storage, one row per (vintage, region, cluster). Replaces the former \code{start}, \code{end} and \code{olife} slots. A vintage is a separately investable variant that keeps the characteristics of its build year for its whole life -- for storage typically a falling capex and a rising round-trip efficiency. Each column is read independently, so a region-agnostic \code{start} may be combined with a per-region \code{olife}.
\describe{
\item{vintage}{character. Vintage label, normally the build year as a string. NA for an un-vintaged storage.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{cluster}{character. Cluster label, NA for every cluster.}
\item{start}{integer. The first year the storage can be installed. Defaults to the vintage year.}
\item{end}{integer. The last year the storage can be installed. Defaults to the vintage year.}
\item{olife}{integer. Operational life of the storage in years, applicable to new investment only.}
}}

\item{\code{capacity}}{data.frame. Capacity parameters of the storage technology.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{cap}{numeric. Capacity of the storage technology.}
\item{cap.lo}{numeric. Lower bound of the storage capacity.}
\item{cap.up}{numeric. Upper bound of the storage capacity.}
\item{cap.fx}{numeric. Fixed value of the storage capacity. This parameter overrides \code{cap.lo} and \code{cap.up}.}
\item{ncap.lo}{numeric. Lower bound of the new storage capacity.}
\item{ncap.up}{numeric. Upper bound of the new storage capacity.}
\item{ncap.fx}{numeric. Fixed value of the new storage capacity. This parameter overrides \code{ncap.lo} and \code{ncap.up}.
}
\item{ret.lo}{numeric. Lower bound of the storage capacity retirement.}
\item{ret.up}{numeric. Upper bound of the storage capacity retirement.}
\item{ret.fx}{numeric. Fixed value of the storage capacity retirement. This parameter overrides \code{ret.lo} and \code{ret.up}.
}
}}

\item{\code{charge}}{data.frame. Pre-charged level at the beginning of the operational cycle.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{slice}{character. Time slice for which the charged level will be specified.}
\item{charge}{numeric. Pre-charged or targeted level at the specified slice.}
}}

\item{\code{seff}}{data.frame. Storage efficiency parameters.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{slice}{character. Time slice to apply the parameter, NA for every slice.}
\item{stgeff}{numeric. Storage decay annual rate.}
\item{inpeff}{numeric. Input efficiency rate.}
\item{outeff}{numeric. Output efficiency rate.}
}}

\item{\code{af}}{data.frame. Availability factor parameters.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{slice}{character. Time slice to apply the parameter, NA for every slice.}
\item{af.lo}{numeric. Lower bound of the availability factor.}
\item{af.up}{numeric. Upper bound of the availability factor.}
\item{af.fx}{numeric. Fixed value of the availability factor. This parameter overrides \code{af.lo} and \code{af.up}.}
\item{cinp.lo}{numeric. Lower bound of the input commodity availability factor.}
\item{cinp.up}{numeric. Upper bound of the input commodity availability factor.}
\item{cinp.fx}{numeric. Fixed value of the input commodity availability factor. This parameter overrides \code{cinp.lo} and \code{cinp.up}.
}
\item{cout.lo}{numeric. Lower bound of the output commodity availability factor.}
\item{cout.up}{numeric. Upper bound of the output commodity availability factor.}
\item{cout.fx}{numeric. Fixed value of the output commodity availability factor. This parameter overrides \code{cout.lo} and \code{cout.up}.
}
}}

\item{\code{aeff}}{data.frame. Auxiliary commodities efficiency parameters.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{acomm}{character. Name of the auxiliary commodity (used in sets).}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{slice}{character. Time slice to apply the parameter, NA for every slice.}
\item{stg2ainp}{numeric. Storaage-level-to-auxiliary-input-commodity coefficient (multiplier).}
\item{cinp2ainp}{numeric. Input-commodity-to-auxiliary-input-commodity coefficient (multiplier).}
\item{cout2ainp}{numeric. Output-commodity-to-auxiliary-input-commodity coefficient (multiplier).}
\item{stg2aout}{numeric. Storage-level-to-auxiliary-output-commodity coefficient (multiplier).}
\item{cinp2aout}{numeric. Input-commodity-to-auxiliary-output-commodity coefficient (multiplier).}
\item{cout2aout}{numeric. Output-commodity-to-auxiliary-output-commodity coefficient (multiplier).}
\item{cap2ainp}{numeric. Capacity-to-auxiliary-input-commodity coefficient (multiplier).}
\item{cap2aout}{numeric. Capacity-to-auxiliary-output-commodity coefficient (multiplier).}
\item{ncap2ainp}{numeric. New-capacity-to-auxiliary-input-commodity coefficient (multiplier).}
\item{ncap2aout}{numeric. New-capacity-to-auxiliary-output-commodity coefficient (multiplier).}
\item{ncap2stg}{numeric. New-capacity-to-storage-level coefficient (multiplier).}
}}

\item{\code{fixom}}{data.frame. Fixed operation and maintenance cost.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{fixom}{numeric. Fixed operation and maintenance cost for the specified sets.}
}}

\item{\code{varom}}{data.frame. Variable operation and maintenance cost.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{slice}{character. Time slice to apply the parameter, NA for every slice.}
\item{inpcost}{numeric. Costs associated with the input commodity.}
\item{outcost}{numeric. Costs associated with the output commodity.}
\item{stgcost}{numeric. Costs associated with the storage level.}
}}

\item{\code{invcost}}{data.frame. Investment cost.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{invcost}{numeric. Overnight investment cost for the specified region and year.}
\item{wacc}{numeric. Weighted average cost of capital used to annuitise \code{invcost} for this storage. Overrides the model-wide \code{wacc} (see the model \code{discount} argument). The social discount rate is never used here.}
\item{payback}{numeric. Cost-recovery period in years. Where given it replaces \code{olife} in the annuity AND in the years over which the annuity is charged, so the investment is repaid over \code{payback} years while the capacity keeps operating for its full operational life. Must be positive and not exceed \code{olife}. Unset (or 0) means recover over \code{olife}. Implemented for the GLPK solver only.}
\item{eac}{numeric. Equivalent annual cost, supplied directly instead of being computed from \code{invcost}, \code{wacc} and the lifetime. Where given it wins; where absent the annuity is computed. Mutually exclusive with \code{invcost} per row.}
\item{retcost}{numeric. Costs of early retirement of the storage, default is 0.}
}}

\item{\code{fullYear}}{logical. If TRUE (default), the storage technology operates between parent timeframes through the year. The last time-slice in the timeframe is used as a preciding time-slice for the first time-slice in the the same group of time-slices within the parent timeframe. if FALSE, the storage charge and discchare cycle is limited to the parent timeframe. The last time-slice in the timeframe is used as a preciding time-slice for the first time-slice in the the same group of time-slices within the parent timeframe.}

\item{\code{cap2stg}}{data.frame. Charging and discharging capacity to the storing capacity inverse ratio, i.e. the storage duration. A data.frame rather than a scalar so it can differ by cluster -- 1h / 4h / 8h duration classes of the same storage. A bare scalar (\code{cap2stg = 4}) is still accepted.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster.}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{cap2stg}{numeric. Capacity-to-storage ratio (storage duration).}
}}

\item{\code{weather}}{data.frame. Weather factors multipliers.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster label selecting the variant this row applies to, NA for every cluster. See the \code{cluster} slot.}
\item{weather}{character. Name of the weather factor to apply.}
\item{waf.lo}{numeric. Coefficient that links the weather factor with the lower bound of the availability factor.}
\item{waf.up}{numeric. Coefficient that links the weather factor with the upper bound of the availability factor.}
\item{waf.fx}{numeric. Coefficient that links the weather factor with the fixed value of the availability factor. This parameter overrides \code{waf.lo} and \code{waf.up}.
}
\item{wcinp.lo}{numeric. Coefficient that links the weather factor with the lower bound of the input commodity availability factor.
}
\item{wcinp.up}{numeric. Coefficient that links the weather factor with the upper bound of the input commodity availability factor.
}
\item{wcinp.fx}{numeric. Coefficient that links the weather factor with the fixed value of the input commodity availability factor. This parameter overrides \code{wcinp.lo} and \code{wcinp.up}.
}
\item{wcout.lo}{numeric. Coefficient that links the weather factor with the lower bound of the output commodity availability factor.
}
\item{wcout.up}{numeric. Coefficient that links the weather factor with the upper bound of the output commodity availability factor.
}
\item{wcout.fx}{numeric. Coefficient that links the weather factor with the fixed value of the output commodity availability factor. This parameter overrides \code{wcout.lo} and \code{wcout.up}.
}
}}

\item{\code{optimizeRetirement}}{logical. Incidates if the retirement of the storage should be optimized. Also requires the same parameter in the \code{model} or \code{scenario} class to be set to TRUE to be effective.}

\item{\code{misc}}{list. List of additional parameters that are not used in the model but can be used for reference or user-defined functions. For example, links to the source of the storage data, or other metadata.}
}}

\concept{process storage}
