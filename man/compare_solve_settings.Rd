% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_size.R
\name{compare_solve_settings}
\alias{compare_solve_settings}
\title{Compare interpolation settings AND solvers for a model}
\usage{
compare_solve_settings(
  mod,
  settings = NULL,
  solvers,
  ...,
  name = "cmp",
  verbose = FALSE,
  tmp.dir = NULL
)
}
\arguments{
\item{mod}{a \code{model}.}

\item{settings}{a named list of \code{interp_mod()} setting combinations (see
\code{\link[=compare_interp_settings]{compare_interp_settings()}}); \code{NULL} uses the default grid.}

\item{solvers}{a list of solver-option objects, e.g.
\code{list(solver_options$glpk, solver_options$julia_highs)}. Names are taken
from the list names when present, otherwise from each option's \verb{$name}.}

\item{...}{forwarded to every \code{interp_mod()} build (e.g.
\code{horizon = newHorizon(period = 2024)}).}

\item{name}{base scenario name.}

\item{verbose}{forwarded to \code{interp_mod()}.}

\item{tmp.dir}{solver working directory (passed to \code{\link[=solve_scen]{solve_scen()}}); \code{NULL}
lets the solver pick one.}
}
\value{
an object of class \code{solve_settings_cmp}: \code{summary} (one row per
setting x solver: ok, seconds, objective, error), \code{interp} (the interp-size
table), plus \code{top} / \code{details} from the interpolation comparison.
}
\description{
Interpolates \code{mod} under each setting combination (via
\code{\link[=compare_interp_settings]{compare_interp_settings()}}) and then solves every build with each supplied
solver option, tabulating the objective, solve time and status. Because the
solution must not depend on storage settings, the objective should be
identical across \code{fold} / \code{sparse} / \code{prune} for a given solver -- the print
method flags any per-solver disagreement.
}
\examples{
\dontrun{
compare_solve_settings(mod,
  solvers = list(solver_options$glpk),
  horizon = newHorizon(period = 2024))
}
}
\seealso{
\code{\link[=compare_interp_settings]{compare_interp_settings()}}, \code{\link[=solve_scen]{solve_scen()}}, \code{\link[=model_size]{model_size()}}
}
