% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/start_level.R
\name{.cycle_first_slices}
\alias{.cycle_first_slices}
\title{Cycle-first timeslices for one storage}
\usage{
.cycle_first_slices(succ, share)
}
\arguments{
\item{succ}{data.frame with \code{timeslice} -> \code{timeslicep} (the SUCCESSOR map)}

\item{share}{named numeric: timeslice -> share of the year, in calendar order}
}
\value{
data.frame with \code{timeslice} (the cycle's first) and \code{share}
}
\description{
Returns one row per closed cycle: its first timeslice (earliest in calendar
order) and its SHARE of the year, which is the sum of its members' shares --
the cycle's parent share by construction. A year-long cycle sums to 1; each
of 365 daily cycles sums to 1/365; a calendar filtered to part of a year sums
to that \code{year_fraction}.
}
\keyword{internal}
