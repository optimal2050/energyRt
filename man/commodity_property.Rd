% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commodity_properties.R
\name{commodity_property}
\alias{commodity_property}
\title{Physical property of a commodity}
\usage{
commodity_property(
  x,
  property = NULL,
  stat = c("value", "range", "min", "max", "sd", "unit", "dist", "comment", "row")
)
}
\arguments{
\item{x}{a \code{commodity} object.}

\item{property}{character, name of the property, e.g. \code{"lhv"}. If \code{NULL}
(the default), the whole property table is returned.}

\item{stat}{character, which part of the row to return. \code{"value"} (the
default) gives the deterministic point estimate; \code{"range"} gives
\code{c(min, max)}; \code{"row"} gives the whole row as a one-row data.frame.}
}
\value{
For \code{stat = "value"}, \code{"min"}, \code{"max"} or \code{"sd"} a numeric of length
one; for \code{"range"} a numeric of length two; for \code{"unit"}, \code{"dist"} or
\code{"comment"} a character of length one; for \code{"row"} a one-row data.frame.
Missing properties give \code{NA} (or a zero-row data.frame for \code{"row"}).
}
\description{
Read a single property out of \code{commodity@property}, or the whole table.
Safe on commodity objects created before the slot existed.
}
\examples{
COA <- newCommodity("COA",
  unit = "PJ",
  property = data.frame(
    property = c("lhv", "density"),
    value = c(25.8, 0.85),
    min = c(24.1, 0.75),
    max = c(27.2, 0.95),
    dist = c("uniform", "triangular"),
    unit = c("GJ/t", "t/m3")
  )
)
commodity_property(COA, "lhv")
commodity_property(COA, "lhv", "range")
commodity_property(COA)
}
\seealso{
\code{\link[=commodity_properties]{commodity_properties()}}, \code{\link[=newCommodity]{newCommodity()}}

Other commodity:
\code{\link[=commodity_properties]{commodity_properties()}},
\code{\link[=newCommodity]{newCommodity()}},
\code{\link[=object_image]{object_image()}}
}
\concept{commodity}
