% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_gams.R, R/write_glpk.R, R/write_jump.R,
%   R/write_pyomo.R
\name{set_gams_path}
\alias{set_gams_path}
\alias{get_gams_path}
\alias{set_gdxlib_path}
\alias{get_gdxlib_path}
\alias{set_glpk_path}
\alias{get_glpk_path}
\alias{set_julia_path}
\alias{get_julia_path}
\alias{set_python_path}
\alias{get_python_path}
\title{Set GAMS and GDX library directory}
\usage{
set_gams_path(path = NULL)

get_gams_path()

set_gdxlib_path(path = NULL)

get_gdxlib_path()

set_glpk_path(path = NULL)

get_glpk_path()

set_julia_path(path = NULL)

get_julia_path()

set_python_path(path = NULL)

get_python_path()
}
\arguments{
\item{path}{character path to the python installation. If NULL, the global operation path is used.}
}
\value{
Sets path to GAMS library in R-options

The current path to GAMS library, set in R-options

Sets path to GDX library in R-options

The current path to GDX library, set in R-options

sets the path to the GLPK library in R options and returns NULL.

returns the path to the GLPK library.

Sets the path to Julia installation in the energyRt environment options and
returns NULL.

character. Path to Julia installation.

Writes or reads the path to python installation or environment
to/from \code{energyRt} options.
}
\description{
This (optional) function sets path to GAMS directory to R-options. It might be useful if for the cases when several different version (and licenses) of GAMS installed, to easily switch between them. It is also possible to set different path for GAMS and GAMS Data Exchange (GDX) libraries.
If GDX path is not set, the GAMS path will be used. If GAMS path is not set, the default system GAMS-path (OS environment variables) instead.
}
\details{
By default energyRt auto-detects the \code{glpsol} executable on the
session \code{PATH} (via \code{\link[base:Sys.which]{base::Sys.which()}}). On Windows this picks up the copy
\strong{bundled with Rtools} (Rtools ships the GLPK dev kit), so an Rtools user
needs no separate GLPK install. Use \code{set_glpk_path()} only to point at a
standalone GLPK installation, which then takes precedence over auto-detection.
}
\examples{
# set_gams_path("C:/GAMS/win64/32.2/")

# get_gams_path()
# set_gdxlib("C:/GAMS/35")
# get_gdxlib()
\dontrun{
set_glpk_path("/usr/local/bin/glpk") # Linux & Mac
set_glpk_path("C:/Program Files/glpk/bin") # Windows
get_glpk_path()
}
\dontrun{
set_julia_path("C:/Program Files/Julia-1.10.1/bin/")
get_julia_path()
}
\dontrun{
set_python_path("C:/Python3")
set_python_path()
get_python_path()
}
}
\concept{GAMS GDX solver}
\concept{solver glpk}
\concept{solver julia}
\concept{solver python}
