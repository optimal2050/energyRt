% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_vintages.R
\name{combine_vintages}
\alias{combine_vintages}
\title{Combine per-vintage technology objects into one vintaged technology}
\usage{
combine_vintages(
  techs,
  vintages = NULL,
  name = NULL,
  desc = NULL,
  close_windows = TRUE,
  collapse_common = TRUE
)
}
\arguments{
\item{techs}{A named list of \code{technology} objects, one per vintage.
Objects declaring clusters are not supported.}

\item{vintages}{Optional character vector of vintage labels (same
length as \code{techs}). Default: parsed from \code{names(techs)}
(\code{"..._VIN<label>"}), falling back to each object's
\verb{@vintage$start}.}

\item{name, desc}{Name/description of the combined technology.
Defaults: the common \code{"..._VIN"} prefix of the input names, and the
first object's description.}

\item{close_windows}{Close open-ended investment windows at the next
vintage's start (default \code{TRUE}).}

\item{collapse_common}{Collapse rows identical across all vintages to
a single all-vintages row (default \code{TRUE}).}
}
\value{
A \code{technology} object with one \verb{@vintage} row per input.
}
\description{
Merges a list of \code{technology} objects, one per build-year vintage
(e.g. \code{FDAC_S_VIN2030}, \code{FDAC_S_VIN2040}, ...), into a single
technology with a \code{\link[=newTechnology]{@vintage}} table and vintage-keyed
parameter rows — the "one object, many vintages" form introduced with
the vintage machinery.
}
\details{
\itemize{
\item Ports (\code{input}/\code{output}/\code{aux}/\code{group}) are the union across
vintages (a commodity present only in some vintages is fine: its
\code{ceff} rows carry the vintage key).
\item Structural attributes (\code{units}, \code{cap2act}, \code{timeframe}, \code{region},
\code{fullYear}) are taken from the first object; differences warn.
\item Every parameter row is tagged with its object's vintage; rows that
are identical across ALL vintages collapse to one all-vintages row
(disable with \code{collapse_common = FALSE}).
\item Investment windows: each vintage's \code{start} is kept (defaulting to
the vintage year); with \code{close_windows = TRUE} a missing \code{end} is
closed at the next vintage's start minus one (the last stays open).
}
}
\examples{
\dontrun{
dac <- make_dea_dac_techs()   # FDAC_S_VIN2020, FDAC_S_VIN2030, ...
fam <- split(dac, sub("_VIN[0-9]+$", "", names(dac)))
dac1 <- lapply(fam, combine_vintages)
draw(dac1$FDAC_S)             # vintage fan
}
}
