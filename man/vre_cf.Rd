% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vre-data.R
\docType{data}
\name{vre_cf}
\alias{vre_cf}
\title{Hourly wind and solar capacity factors, one site, one year}
\format{
A data.frame of 8760 rows and three columns:
\describe{
\item{slice}{timeslice, \code{"d001_h00"} to \code{"d365_h23"}.}
\item{solar}{solar capacity factor, 0-1.}
\item{wind}{wind capacity factor, 0-1.}
}
}
\source{
Derived from the NASA MERRA-2 reanalysis with the \code{merra2ools}
package. See \code{data-raw/vre_cf.R} for the generating script.
}
\usage{
vre_cf
}
\description{
A full year of hourly capacity factors for one solar and one wind resource
at a single site, for examples that need a realistic sub-annual profile
rather than a synthetic one -- storage sizing, curtailment and
\code{\link[=storage_duration]{storage_duration()}} in particular.
}
\details{
The site is deliberately unlabelled: the data carries no coordinates, region
code or name. It is one plausible pair of contrasting profiles, not a claim
about a particular place. Solar is a fixed-tilt profile, wind a 100 m hub
height; each comes from a single resource cluster.

\code{slice} is already in the \code{d365_h24} vocabulary, so the table joins directly
onto \code{calendars$d365_h24} with no conversion.
}
\examples{
str(vre_cf)
# annual mean capacity factor of each resource
colMeans(vre_cf[, c("solar", "wind")])
}
\seealso{
\link{calendars}, \code{\link[=storage_duration]{storage_duration()}}
}
\keyword{datasets}
