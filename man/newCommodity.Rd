% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-commodity.R
\name{newCommodity}
\alias{newCommodity}
\title{Create new commodity object}
\usage{
newCommodity(
  name = "",
  desc = "",
  limtype = "LO",
  timeframe = character(),
  geolevel = character(),
  unit = character(),
  agg = data.frame(),
  emis = data.frame(),
  property = data.frame(),
  misc = list(),
  image = NULL,
  icon = NULL
)
}
\arguments{
\item{name}{character. Name of the commodity.}

\item{desc}{character. Optional description of the commodity for reference.}

\item{limtype}{factor or character. The limit type of the commodity in balance equation, "LO", "UP", or "FX". "LO" by default, meaning that the level of commodity in the model is restricted with the lower bound, excess is allowed. "UP" means that the level of commodity cannot exceed the upper bound. "FX" means that total commudity supply and demand are equal, no excess or deficit is allowed.}

\item{timeframe}{character. The default time-frame this commodity operates in the model. The lowest timeframe in the model is used by default.}

\item{geolevel}{character. The geoscale level at which this commodity is balanced -- the spatial counterpart of \code{timeframe}. The finest level in the model is used by default, reproducing the flat, single-level behaviour. Naming a coarser level (e.g. balancing steel nationally while electricity stays state-level) asserts free, unlimited transport of the commodity within that level, so it suits goods with a genuinely integrated market and never a network-constrained carrier such as electricity. Requires a geoscale on the model config; GLPK and GAMS only.}

\item{unit}{character. The main unit of the commodity used in the model.}

\item{agg}{data.frame. Used to define an aggregation of several commodities into the \code{name} commodity.

\describe{
\item{comm}{character. Name of a commodity being aggregated.}
\item{unit}{character. Unit of the commodity being aggregated.}
\item{agg}{numeric. weight of the commodity in the aggregation, must be set for all aggregated commodities.
}
}}

\item{emis}{data.frame. Emissions factors related to the commodity consumption (if "combustion" parameter of a technology which consumes the commodity is > 0).

\describe{
\item{comm}{character. Name of the emitted commodity.}
\item{unit}{character. Unit of the emission factor.}
\item{emis}{numeric. Emission factor, emissions released per unit of the consumed commodity.
}
}}

\item{property}{data.frame. Physical properties of the commodity -- heating values, density, molar mass, composition. Reference data: it is never written to the model input, but it gives \code{convert()} the physics needed to move between measures of the same commodity (energy, mass, volume, amount). A property whose unit is a ratio of two dimensions, such as \code{lhv} in "GJ/t", acts as a conversion edge between them. Only properties of the commodity itself belong here -- anything whose numerator is a \emph{different} commodity, such as a CO2 emission factor, belongs in \code{emis}. See \code{commodity_properties()} for the recognised names.

\describe{
\item{property}{character. Name of the property, e.g. "lhv", "hhv", "density", "molar_mass", or "frac_C" for an element mass fraction. Unrecognised names are kept with a warning.
}
\item{value}{numeric. The value of the property, in \code{unit}. Always the deterministic point estimate -- the mean, mode or best guess; \code{min}, \code{max} and \code{sd} describe uncertainty around it and are never read by \code{convert()}.
}
\item{min}{numeric. Lower bound of the value, for sensitivity analysis. Optional.}
\item{max}{numeric. Upper bound of the value, for sensitivity analysis. Optional.}
\item{sd}{numeric. Standard deviation of the value, for sensitivity analysis. Optional.
}
\item{dist}{character. Distribution of the value for sensitivity analysis, one of "point", "uniform", "triangular", "pert", "normal", or "lognormal". Optional; "point" or NA means the value is treated as certain. "uniform" reads \code{min} and \code{max}; "triangular" and "pert" read \code{min}, \code{value} (the mode) and \code{max}; "normal" and "lognormal" read \code{value} and \code{sd}.
}
\item{unit}{character. Unit of the property, e.g. "GJ/t", "t/m3", "g/mol", "kg/kg". Applies to \code{value}, \code{min}, \code{max} and \code{sd} alike.
}
\item{comment}{character. Optional note on the source of the value or the conditions it refers to, such as "bulk, as stockpiled" or "gas, 0 C, 1 atm".
}
}}

\item{misc}{list. List of additional parameters that are not used in the model but can be used for reference or user-defined functions. For example, links to the source of the commodity data, or other metadata. Two names are conventional: \code{misc$image}, a path or URL to an illustration used in reports, and \code{misc$icon}, a path or URL to a small icon. Both can be set with the \code{image} and \code{icon} arguments of \code{newCommodity}.}

\item{image}{character, optional path or URL to an illustration of the
commodity, used in reports. Stored in \code{misc$image}.}

\item{icon}{character, optional path or URL to a small icon for the
commodity. Stored in \code{misc$icon}.}
}
\value{
commodity object
}
\description{
Create new commodity object
}
\examples{
newCommodity(name = "ELC", desc = "Electricity", unit = "GWh")

# physical properties give `convert()` the physics to move between measures
newCommodity(
  name = "COA", desc = "Steam coal", unit = "PJ",
  property = data.frame(
    property = c("lhv", "density"),
    value = c(25.8, 0.85),
    unit = c("GJ/t", "t/m3"),
    comment = c("IEA average, other bituminous", "bulk, as stockpiled")
  ),
  emis = data.frame(comm = "CO2", unit = "Mt", emis = 0.0946)
)
}
\seealso{
Other commodity:
\code{\link[=commodity_properties]{commodity_properties()}},
\code{\link[=commodity_property]{commodity_property()}},
\code{\link[=object_image]{object_image()}}
}
\concept{commodity}
