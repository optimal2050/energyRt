% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-config.R, R/geoscale.R
\name{setGeoscale,config-method}
\alias{setGeoscale,config-method}
\alias{getGeoscale,config-method}
\alias{setGeoscale}
\alias{getGeoscale}
\alias{getCalendar}
\alias{setGeoscale,model-method}
\alias{getGeoscale,model-method}
\alias{setGeoscale,scenario-method}
\alias{getGeoscale,scenario-method}
\alias{getCalendar,config-method}
\alias{getCalendar,model-method}
\alias{getCalendar,scenario-method}
\title{Attach or read a model's geoscale}
\usage{
\S4method{setGeoscale}{config}(obj, geoscale, ...)

\S4method{getGeoscale}{config}(obj, ...)

\S4method{setGeoscale}{model}(obj, geoscale, ...)

\S4method{getGeoscale}{model}(obj, ...)

\S4method{setGeoscale}{scenario}(obj, geoscale, ...)

\S4method{getGeoscale}{scenario}(obj, ...)

\S4method{getCalendar}{config}(obj)

\S4method{getCalendar}{model}(obj)

\S4method{getCalendar}{scenario}(obj)
}
\arguments{
\item{obj}{A \code{config}, \code{settings}, \code{model} or \code{scenario}.}

\item{geoscale}{A \code{geoscales::Geoscale}, or \code{NULL} to clear it.}

\item{...}{Unused.}
}
\value{
\code{setGeoscale()} returns the modified object; \code{getGeoscale()} returns
a \code{geoscales::Geoscale} or \code{NULL}.
}
\description{
A geoscale describes the model's regions -- their nesting into coarser
levels, their weights, and optionally their geometry. It is built with the
\href{https://github.com/optimal2050/geoscales}{geoscales} package and is used
for plotting, reporting and subsetting only: attaching one never changes the
optimisation model.
}
\details{
\code{setGeoscale()} stores it on a \code{config} (and therefore on a \code{settings}, which
inherits from \code{config}) or on a \code{model}. \code{getGeoscale()} reads it back;
for a \code{scenario} it reads \verb{@settings} first and falls back to the model's
\verb{@config}, matching how \verb{@calendar} is resolved.
}
\examples{
\dontrun{
gs <- geoscales::geoscale_from_leaftable(
  data.frame(zone = c("N", "N", "S"), region = c("R1", "R2", "R3")),
  geoframes = c("zone", "region")
)
mod <- newModel("demo", region = c("R1", "R2", "R3"), geoscale = gs)
getGeoscale(mod)
}

}
\seealso{
Other geoscale:
\code{\link[=check_geoscale_regions]{check_geoscale_regions()}},
\code{\link[=plot_map]{plot_map()}},
\code{\link[=utopia_geoscale]{utopia_geoscale()}}
}
\concept{geoscale}
