% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-trade.R
\name{newTrade}
\alias{newTrade}
\title{Create new trade object}
\usage{
newTrade(
  name = "",
  desc = "",
  commodity = character(),
  routes = data.frame(),
  trade = data.frame(),
  fixom = data.frame(),
  varom = data.frame(),
  cluster = data.frame(),
  invcost = data.frame(),
  olife = data.frame(),
  start = data.frame(start = -Inf, stringsAsFactors = FALSE),
  end = data.frame(end = Inf, stringsAsFactors = FALSE),
  vintage = data.frame(),
  capacity = data.frame(),
  aux = data.frame(),
  aeff = data.frame(),
  cap2act = 1,
  optimizeRetirement = FALSE,
  misc = list(),
  ...
)
}
\arguments{
\item{name}{character. Name of the trade object, used in sets.}

\item{desc}{character. Description of the trade object.}

\item{commodity}{character. The traded commodity short name.}

\item{routes}{data.frame. Source and destination regions. For bivariate trade define both directions in separate rows.
\describe{
\item{from}{character. Source region.}
\item{to}{character. Destination region.}
}}

\item{trade}{data.frame. Technical parameters of trade per directed route. Two kinds of flow bound live here and they are not interchangeable: \verb{ava.*} is ABSOLUTE, in physical commodity units per timeslice, and does not scale with capacity; \verb{af.*} is RELATIVE, a fraction of the object's own capacity (\code{cap2act} x \code{vTradeCap} x the timeslice share), and is the direct analogue of \code{technology@af}. Use \verb{af.*} to rate individual lines of a multi-route object, where the absolute bound would have to be a number correct for exactly one capacity -- and capacity is the decision.

\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster (loss tranche) label selecting the tranche this row applies to, NA for every tranche. See the \code{cluster} slot.}
\item{src}{character. Source region of the flow, NA for every source region on the route.}
\item{dst}{character. Destination region of the flow, NA for every destination region on the route.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{timeslice}{character. Time timeslice to apply the parameter, NA for every timeslice.}
\item{ava.lo}{numeric. Lower bound on the traded flow from \code{src} to \code{dst}, in physical commodity units per timeslice.
}
\item{ava.up}{numeric. Upper bound on the traded flow from \code{src} to \code{dst}, in physical commodity units per timeslice.
}
\item{ava.fx}{numeric. Fixed value of the traded flow from \code{src} to \code{dst}, in physical commodity units per timeslice. This parameter overrides \code{ava.lo} and \code{ava.up}.
}
\item{af.lo}{numeric. Lower bound on the flow from \code{src} to \code{dst} as a FRACTION of the object's capacity: \verb{af.lo x cap2act x vTradeCap x share}. Forces a minimum loading on that direction.
}
\item{af.up}{numeric. Upper bound on the flow from \code{src} to \code{dst} as a FRACTION of the object's capacity: \verb{af.up x cap2act x vTradeCap x share}. This is how a single trade object carrying several routes gives each line its own rating -- \code{af.up = 0.5} on one leg limits it to half the corridor whatever the corridor turns out to be. Note that \code{af.up} of 1 is already implied by \code{eqTradeCapFlow} and cannot bind.
}
\item{af.fx}{numeric. Fixed loading of the direction \code{src} -> \code{dst} as a fraction of capacity. Overrides \code{af.lo} and \code{af.up}.
}
\item{teff}{numeric. Trade efficiency: the fraction of the flow sent from \code{src} that is delivered to \code{dst} (1 = lossless).
}
\item{reactance}{numeric. Series reactance \code{x} of the line, in the model's own impedance units. Carried for AC lines and used by nothing unless the model is interpolated with \code{kvl = TRUE}, when it becomes the coefficient of Kirchhoff's voltage law. Presence of a finite \code{reactance} is what marks a route as a PASSIVE AC branch: a controllable DC link has none, because its flow is chosen rather than set by impedance. Must be SYMMETRIC across the two directed rows of a line -- the voltage law constrains the net flow -- and a line under KVL must be declared in both directions. Where several physical circuits have been merged into one route, the value is the EQUIVALENT reactance, \code{1/x_eq = sum(1/x_i)}, not any one circuit's.
}
\item{resistance}{numeric. Series resistance \code{r} of the line, same units and same symmetry requirement as \code{reactance}. Recorded so a line keeps both parameters through a round trip. It does not drive losses by itself -- losses are the per-route \code{teff} -- but it is the input \code{lossTranches()} turns into a piecewise-linear loss curve: pass it with the rating it was measured against, since \code{loss_full = r * F} and a resistance alone does not determine a loss fraction.
}
}}

\item{fixom}{data.frame. Fixed operation and maintenance costs, per unit of standing capacity per year. Like the other trade cost slots this is a RATE PER ENDPOINT REGION, named in its \code{region} column -- see the note on \code{invcost}.}

\item{varom}{data.frame. Costs charged on the traded flow, PER ROUTE. Unlike the capacity costs above -- which are a rate per endpoint region -- these are indexed by \verb{(src, dst, year, timeslice)}, so they can differ by direction. The two columns mean different things:

\code{varom}  -- variable operation and maintenance, a REAL resource cost.
Charged where the flow ORIGINATES (\code{src}) and added to
total system cost, exactly as \code{technology@varom} is. Use
it for wheeling fees, transit taxes or any real charge per
unit shipped. Becomes \code{pTradeIrCost}.

\code{markup} -- a border price or bilateral charge: a TRANSFER. The
importing region pays it and the exporting region
receives it, so it moves cost between regions and leaves
the objective unchanged -- the same distinction as a tax
versus a cap. Becomes \code{pTradeIrMarkup}.

Before 0.85 both columns were summed into a single term that cancelled between \code{eqImportIrCost} and \code{eqExportIrCost}, so \code{varom} could not express an operating cost at all; it now enters the objective once, positively, at \code{src}. (A separate parameter \code{pTradeVarom} was once declared in the GLPK and GAMS templates and read by no equation; it has been removed and was never this slot.)}

\item{invcost}{data.frame. Investment cost of the trade capacity, as a RATE PER ENDPOINT REGION per unit of capacity. Trade capacity is region-free -- one number per corridor -- but its costs are region-indexed, and each region named in \code{region} pays its own rate on the whole capacity. A corridor whose two endpoints each pay 100 therefore costs 200 in total. Leaving \code{region} unset applies the rate at every endpoint of the route; energyRt reports that rather than correcting it, because the same rate is what a partial-region study would see -- solve a subset of the endpoints and it bears only their share, which is the intended behaviour. Name the regions to vary the rate between them. \code{fixom}, \code{retcost} and \code{eac} follow the same convention.

\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster (loss tranche) label selecting the tranche this row applies to, NA for every tranche. See the \code{cluster} slot.}
\item{region}{character. Endpoint region bearing this rate. NA applies the rate at every endpoint of the route; name the regions to vary it between them. Must be one of the model's own regions -- a coarser geoscale level is accepted by validation but never reaches the objective.
}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{invcost}{numeric. Investment cost.}
\item{wacc}{numeric. Weighted average cost of capital used to annuitise \code{invcost} for this corridor. Overrides the model-wide \code{wacc} (see the model \code{discount} argument). The social discount rate is never used here.}
\item{payback}{numeric. Cost-recovery period in years. Where given it replaces \code{olife} in the annuity AND in the years over which the annuity is charged, so the investment is repaid over \code{payback} years while the capacity keeps operating for its full operational life. Must be positive and not exceed \code{olife}. Unset (or 0) means recover over \code{olife}. Implemented for the GLPK solver only.}
\item{eac}{numeric. Equivalent annual cost, supplied directly instead of being computed from \code{invcost}, \code{wacc} and the lifetime. Where given it wins; where absent the annuity is computed. Mutually exclusive with \code{invcost} per row.}
\item{retcost}{numeric. Costs of early retirement of the trade capacity, default is 0.}
}}

\item{olife}{deprecated, use the \code{olife} column of \code{vintage}.}

\item{start}{deprecated, use the \code{start} column of \code{vintage}.}

\item{end}{deprecated, use the \code{end} column of \code{vintage}.}

\item{vintage}{data.frame. Investment window and operational life of the trade object, one row per vintage. Replaces the former \code{start}, \code{end} and \code{olife} slots. A vintage is a separately investable variant that keeps the characteristics of its build year for its whole life, so several vintages of one corridor mean several capacities on the same \verb{(src, dst)} route, with their flows summed in the commodity balance. \code{region} is present for a uniform shape across process classes but is unused for trade: its scope comes from the route endpoints. \code{cluster} selects a loss tranche declared in the \code{cluster} slot.
\describe{
\item{vintage}{character. Vintage label, normally the build year as a string. NA for an un-vintaged trade.}
\item{region}{character. Unused for trade (no \code{region} slot); present for consistency with the other classes.}
\item{cluster}{character. Cluster (loss tranche) this row applies to, NA for every tranche. See the \code{cluster} slot.
}
\item{start}{integer. The first year the trade object is available for investment. NA means unbounded (up to \code{end}).}
\item{end}{integer. The last year the trade object is available for investment. NA means unbounded (from \code{start} on).}
\item{olife}{integer. Operational life of the trade object in years.}
}}

\item{capacity}{data.frame. Capacity parameters of the trade object: \code{stock} (the legacy corridor still standing at each milestone), the \verb{cap.*} bounds on total capacity, the \verb{ncap.*} bounds on the build RATE and the \verb{ret.*} bounds on the early retirement rate. Region-free: a route IS a pair of regions, so a per-region capacity would be ambiguous.}

\item{aux}{data.frame. Auxiliary commodity of trade.
\describe{
\item{acomm}{character. Name of the auxiliary commodity (used in sets).}
\item{unit}{character. Unit of the auxiliary commodity.}
}}

\item{aeff}{data.frame. Auxiliary commodity efficiency parameters.
\describe{
\item{vintage}{character. Vintage label selecting the variant this row applies to, NA for every vintage. See the \code{vintage} slot.}
\item{cluster}{character. Cluster (loss tranche) label selecting the tranche this row applies to, NA for every tranche. See the \code{cluster} slot.}
\item{acomm}{character. Name of the auxiliary commodity (used in sets).}
\item{region}{character. Region name to apply the parameter, NA for every region.}
\item{year}{integer. Year to apply the parameter, NA for every year.}
\item{timeslice}{character. Time timeslice to apply the parameter, NA for every timeslice.}
\item{trade2ainp}{numeric. Trade-to-auxiliary-input-commodity coefficient (multiplier).}
\item{trade2aout}{numeric. Trade-to-auxiliary-output-commodity coefficient (multiplier).}
}}

\item{cap2act}{numeric. Capacity to activity ratio.}

\item{optimizeRetirement}{logical. Incidates if the retirement of the trade object should be optimized. Also requires the same parameter in the \code{model} or \code{scenario} class to be set to TRUE to be effective.}

\item{misc}{list. Additional information.}
}
\value{
trade object with given specifications.
}
\description{
Constructor for trade object.
}
\details{
Trade objects are used to represent inter-regional exchange in the model.
Without trade, every region is isolated and can only use its own resources.
The class defines trade routes, efficiency, costs,
and other parameters related to the process. Number of routes per trade object is not
limited. One trade object can have a part or entire trade network of the model.
However, it has a distinct name and all the routs will be optimized together.
Create separate trade objects to optimize different parts of the trade network
(aka transmission lines).
}
\examples{
PIPELINE1 <- newTrade(
  name = "PIPELINE1",
  desc = "Some transport pipeline",
  commodity = "OIL",
  routes = data.frame(
    src = c("R1", "R2"),
    dst = c("R2", "R3")
  ),
  trade = data.frame(
    src = c("R1", "R2"),
    dst = c("R2", "R3"),
    teff = c(0.99, 0.98)
  ),
  olife = list(olife = 60)
)
draw(PIPELINE1)

PIPELINE2 <- newTrade(
  name = "PIPELINE2",
  desc = "Some transport pipeline",
  commodity = "OIL",
  routes = data.frame(
    src = c("R1", "R1", "R2", "R3"),
    dst = c("R2", "R3", "R3", "R2")
  ),
  trade = data.frame(
    src = c("R1", "R1", "R2", "R3"),
    dst = c("R2", "R3", "R3", "R2"),
    teff = c(0.912, 0.913, 0.923, 0.932)
  ),
  aux = data.frame(
    acomm = c("ELC", "CH4"),
    unit = c("MWh", "kt")
  ),
  aeff = data.frame(
    acomm = c("ELC", "CH4", "ELC", "CH4"),
    src = c("R1", "R1", "R2", "R3"),
    dst = c("R2", "R2", "R3", "R2"),
    csrc2ainp = c(.5, NA, .3, NA),
    cdst2ainp = c(.4, NA, .6, NA),
    csrc2aout = c(NA, .1, NA, .2)
  ),
  olife = list(olife = 60)
)
draw(PIPELINE2, node = "R1")
draw(PIPELINE2, node = "R2")
draw(PIPELINE2, node = "R3")
}
\concept{trade process constructor}
