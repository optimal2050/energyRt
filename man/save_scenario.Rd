% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arrow.R
\name{save_scenario}
\alias{save_scenario}
\title{Save scenario object on disk in parquet format using \code{arrow} package.}
\usage{
save_scenario(
  scen,
  path = scen@path,
  embed_model = NULL,
  embed_datasets = NULL,
  format = get_storage_format(),
  overwrite = TRUE,
  clean_start = FALSE,
  write_log = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{scen}{scenario object.}

\item{path}{character. Path to scenario directory.}

\item{embed_model}{\code{NULL} (default), \code{TRUE}, or \code{FALSE}. Controls whether
the model is embedded in the saved scenario or referenced from the model
store (see \code{\link[=save_model]{save_model()}}). \code{NULL}: reference when the identical model
content is already in the store, embed otherwise. \code{TRUE}: always embed
(self-contained folder). \code{FALSE}: require a store hit, error otherwise.
A referenced save stores only \verb{\{name, hash, path\}}; \code{\link[=load_scenario]{load_scenario()}}
resolves it back via the registry / model store.}

\item{embed_datasets}{the same choice for the big data tables and maps —
the geoscale on \verb{@settings} and, when the model is embedded, its
geoscale and its repositories' weather/demand tables (see
\code{\link[=save_dataset]{save_dataset()}}). \code{NULL} (default) references whatever identical
content is already in the dataset store; \code{TRUE} always embeds; \code{FALSE}
requires a store hit.}

\item{format}{file format (currently \code{parquet} only, arrow or feather will be implemented in further releases).}

\item{overwrite}{logical. Overwrite existing scenario directory.}

\item{clean_start}{logical. Clean scenario directory before saving.}

\item{write_log}{logical. Write (update) logfile.}

\item{verbose}{logical. Print messages.}
}
\value{
scenario object with most of the slots saved on disk.
}
\description{
Save scenario object on disk in parquet format using \code{arrow} package.
}
\examples{
\dontrun{
scen_BASE@path # check the scenarion directory
scen_BASE <- save_scenario(scen_BASE) # saving in the default directory
}
}
