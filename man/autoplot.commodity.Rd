% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{autoplot.commodity}
\alias{autoplot.commodity}
\title{Visualize a Commodity object}
\usage{
\method{autoplot}{commodity}(object, ...)
}
\arguments{
\item{object}{A \code{commodity} object.}

\item{...}{Optional further \code{commodity} objects to include in the comparison.}

\item{palette}{Viridis color option for the emission-species fill scale.}
}
\value{
A \code{ggplot} object, or \code{NULL} (invisibly) if there is nothing to plot.
}
\description{
Plots the commodity's emission factors (\verb{@emis}) as bars — one bar per
emission species, with the y-axis in the emission unit (e.g. \code{kt/GWh}).
Additional \code{commodity} objects can be passed via \code{...} to compare emission
intensities side by side (e.g. \code{autoplot(COA, OIL, GAS)}). Commodities with
no emission factors produce a message and return \code{NULL} invisibly.
}
\examples{
\dontrun{
coa <- newCommodity("COA", emis = data.frame(comm = "CO2", unit = "kt/GWh", emis = 0.33))
autoplot(coa)
}
}
