% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-variable.R
\docType{class}
\name{class-variable}
\alias{class-variable}
\title{An S4 class to specify a model variable}
\description{
The counterpart of \link{class-parameter}: a \code{parameter} carries the model's input
data, a \code{variable} its solved output. Every variable declared by the model
has one of these, pre-populated from the baked specification when a \code{modOut}
is created and filled with solved values by \code{read_solution()} -- exactly as
\code{modInp@parameters} is populated from \code{data-raw/modInp.yml}.

The specification itself is composed at build time in \code{data-raw/DATASET.R}:
dimensions, gating map and description come from the GAMS source via gams2x,
the emitted column names and the positive/free declaration are parsed out of
\code{glpk/energyRt.mod}, and \code{role} / \code{unit} / \code{origin} come from the
\code{data-raw/variables.yml} overlay.

The class and its methods are not intended for direct use; read values with
\code{\link[=getData]{getData()}}.
}
\section{Slots}{

\describe{
\item{\code{name}}{character, the variable's name in the model.}

\item{\code{desc}}{character, one-line description.}

\item{\code{dimSets}}{character, the variable's dimensions \strong{as the model declares
them}, duplicates preserved (\code{vTradeIr} is
\verb{trade, comm, region, region, year, slice}). Byte-identical to
\code{.variable_set}, which \code{newCosts()} and \code{newConstraint()} consume
positionally against the GAMS declaration.}

\item{\code{colNames}}{character, the column names the variable is actually written
out with. Differs from \code{dimSets} only where a dimension repeats and the
writers disambiguate it -- \code{src}/\code{dst} for \code{vTradeIr}, \code{yearp} for
\code{vTechRetiredNewCap}.}

\item{\code{gate}}{character, the name of the map that gates the variable's domain
(the \code{$}-condition), or \code{""} when it is unconditional.}

\item{\code{role}}{character, what the variable is: \code{source}, \code{sink}, \code{flow},
\code{activity}, \code{balance}, \code{stock}, \code{capacity} or \code{cost}. A \code{stock} is a level
at a point in time and is never summed over slices.}

\item{\code{unit}}{character, the KIND of quantity (\code{commodity}, \code{activity},
\code{capacity}, \code{currency}), not a unit string -- real units are per-object
here and are resolved against the model.}

\item{\code{positive}}{logical, whether the model declares the variable non-negative.}

\item{\code{origin}}{character, \code{solver} or \code{computed} (built in R by \code{read.R}, so
never expected in solver output).}

\item{\code{data}}{data.frame, the solved values.}

\item{\code{misc}}{list, \code{path} / \code{inMemory} / \code{onDisk} when stored on disk.}
}}

\seealso{
Other variable:
\code{\link[=get_variable]{get_variable()}}
}
\concept{variable}
