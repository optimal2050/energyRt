% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolation.R
\name{get_process_stored}
\alias{get_process_stored}
\title{Stored commodities of each process}
\usage{
get_process_stored(scen, process = NULL, classes = NULL)
}
\arguments{
\item{scen}{scenario object}

\item{process}{optional character vector to restrict to}

\item{classes}{classes to search; defaults to \code{"storage"}}
}
\value{
a named list mapping each process to its stored commodity
}
\description{
The commodity a process HOLDS, as distinct from what it consumes or produces.
Only \code{storage} has one. It is \verb{@storage$comm} where given, otherwise
\verb{@commodity} -- so a storage that names no roles keeps today's behaviour.
}
\details{
Needed because a storage may hold something that is neither its input nor its
output (hydrogen: ELC in, H2 stored, ELC out). Without this the stored
commodity never reaches \code{get_process_comm()}, and the storage's timeframe
would be derived from its ELC flows alone -- putting the level at the wrong
resolution.
}
