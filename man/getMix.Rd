% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scenario.R
\name{getMix}
\alias{getMix}
\title{Extract a tidy mix (generation, capacity, fuel) from a solved scenario}
\usage{
getMix(
  scen,
  type = c("generation", "capacity", "new_capacity", "fuel"),
  comm = "ELC",
  region = NULL,
  year = NULL,
  slice = NULL,
  drop_small = 0
)
}
\arguments{
\item{scen}{a solved \code{scenario} object, or a \strong{named list} of scenarios
(results are row-bound with the list names in the \code{scenario} column).}

\item{type}{character: \code{"generation"} (default -- output of \code{comm} by process,
with storage-in and exports negative), \code{"capacity"} (\code{vTechCap} +
\code{vStorageCap}), \code{"new_capacity"} (\code{vTechNewCap} + \code{vStorageNewCap}), or
\code{"fuel"} (technology fuel consumption \code{vTechInp} by input commodity).}

\item{comm}{character, the balanced commodity for \code{"generation"}
(default \code{"ELC"}). Ignored for the other types.}

\item{region}{character vector or \code{NULL} (all regions). Inter-regional trade
flows are only meaningful for a region subset and are skipped when
\code{region = NULL} (they cancel out in an all-region sum).}

\item{year}{integer vector or \code{NULL} (all milestone years).}

\item{slice}{\code{NULL} for annual sums, or a regular expression selecting a
slice sample (e.g. \code{"^SUM_"} for the summer day on the \code{utopia_s4h24}
calendar). When the matched slices carry an hour tag (\verb{"_h00"..."_h23"}),
an integer \code{hour} column is added.}

\item{drop_small}{numeric in \verb{[0, 1)}: drop processes whose total absolute
value is below this share of the largest process (default \code{0}, keep all).}
}
\value{
A tidy data.frame with columns \code{scenario}, \code{type}, \code{process}, \code{flow}
(\code{generation}, \verb{storage-in/out}, \code{import/export}, \code{demand}, \code{fuel},
\code{capacity}, \code{new_capacity}), \code{comm}, \code{region}, \code{year}, \code{value}, and -- when
\code{slice} is given -- \code{slice} (+ \code{hour} when parsable). Missing variables are
skipped silently (e.g. a model without storage or trade).
}
\description{
Collects the building blocks of a "mix" chart from a solved scenario into one
tidy data.frame: technology output, storage charge/discharge, inter-regional
and rest-of-world trade, and demand. The same extractor feeds
\code{autoplot(scenario, ...)}, the scenario report, and any custom analysis
(including sonification -- see the useR! training course).
}
\examples{
\dontrun{
gen <- getMix(scen, "generation")                      # annual, all regions
day <- getMix(scen, "generation", slice = "^SUM_")     # summer-day dispatch
cmp <- getMix(list(BASE = s1, CO2CAP = s2), "capacity")
}
}
