% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_weather}
\alias{plot_weather}
\alias{autoplot.weather}
\title{Visualize a weather object}
\usage{
plot_weather(
  object,
  style = c("heatmap", "line", "area"),
  calendar = NULL,
  palette = "D",
  datetime = FALSE,
  angle = 45,
  region = NULL,
  ...
)

\method{autoplot}{weather}(
  object,
  style = c("heatmap", "line", "area"),
  calendar = NULL,
  region = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{weather} object.}

\item{style}{One of \code{"heatmap"} (default), \code{"line"}, \code{"area"}.}

\item{calendar}{A \code{calendar} object (or format string) giving the timeslice
layout. Recommended for a fully structured view. If \code{NULL}, the layout is
guessed; when that fails, \code{"<prefix>_h##"}-style timeslices (e.g. season+hour)
are split into a coarse label + hour, otherwise timeslices are shown on a single
ordered axis. In every case region and year (when present) are drawn as
facets.}

\item{palette}{Viridis color option for the heatmap fill.}

\item{datetime}{Logical (line/area only). If \code{TRUE}, place the profile on a
real datetime axis via \code{\link[=tsl2dtm]{tsl2dtm()}}; if the timeslice type is not yet supported
the categorical axis is kept (with a warning).}

\item{angle}{Rotation (degrees) for the x-axis tick labels; overlapping labels
are dropped so dense sub-annual axes stay legible. Default \code{45}; \code{0} = flat.}

\item{region}{Which regions to draw. \code{NULL} (default) draws them all. A
character vector names them; a single number takes that many, in the order
they appear. Useful on converted continental models, where a weather factor
can carry dozens of regions and faceting on all of them leaves the panels
unreadable.}

\item{...}{Reserved for future use.}
}
\value{
A \code{ggplot} object (or \code{NULL}, invisibly, if there is nothing to plot).
}
\description{
Plots the sub-annual weather factor \code{wval} (a capacity / availability factor)
of a \code{weather} object, in one of three styles:
\itemize{
\item \code{"heatmap"} (default) — a calendar heatmap (finest timeframe on \code{y}, next on
\code{x}), faceted by region; the value's unit is on the fill legend;
\item \code{"line"} — the factor against the finest time level (e.g. hour), one line per
coarser level (e.g. season), faceted by region;
\item \code{"area"} — the same as \code{"line"} with filled areas.
}

The value axis (fill for the heatmap, \code{y} for line/area) is labelled with the
object's \verb{@unit} (or \code{"capacity factor"} if unset).
}
\examples{
\dontrun{
data("calendars", package = "energyRt")
W <- getObject(utopia$modules$electricity$R3$repo, name = "WSOL", drop = TRUE)
autoplot(W, calendar = calendars$s4_h24)                     # heatmap
autoplot(W, style = "line", calendar = calendars$s4_h24)
}
}
\keyword{internal}
