% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_data.R, R/get_data_objects.R
\name{getData}
\alias{getData}
\alias{getData.scenario}
\alias{get_data}
\alias{getData.list}
\alias{getData.default}
\alias{getData.technology}
\alias{getData.commodity}
\alias{getData.storage}
\alias{getData.supply}
\alias{getData.demand}
\alias{getData.trade}
\alias{getData.import}
\alias{getData.export}
\alias{getData.weather}
\alias{getData.model}
\alias{getData.repository}
\title{Extract data from energyRt objects}
\usage{
getData(scen, ...)

\method{getData}{scenario}(
  scen,
  name = NULL,
  ...,
  merge = FALSE,
  timeframe = c("lowest", "highest", "all"),
  process = FALSE,
  parameters = TRUE,
  variables = TRUE,
  sets = FALSE,
  maps = FALSE,
  ignore.case = TRUE,
  newNames = NULL,
  newValues = NULL,
  na.rm = FALSE,
  digits = NULL,
  drop.zeros = FALSE,
  add_weights = "auto",
  add_period_length = "auto",
  apply_weights = FALSE,
  apply_period_length = FALSE,
  asTibble = TRUE,
  as_data_table = FALSE,
  stringsAsFactors = FALSE,
  yearsAsFactors = FALSE,
  drop_duplicated_scenarios = TRUE,
  scenNameInList = as.logical(length(scen) - 1),
  unfold = TRUE,
  verbose = FALSE
)

\method{getData}{list}(
  scen,
  name = NULL,
  ...,
  merge = FALSE,
  timeframe = c("lowest", "highest", "all"),
  process = FALSE,
  parameters = TRUE,
  variables = TRUE,
  sets = FALSE,
  maps = FALSE,
  ignore.case = TRUE,
  newNames = NULL,
  newValues = NULL,
  na.rm = FALSE,
  digits = NULL,
  drop.zeros = FALSE,
  add_weights = "auto",
  add_period_length = "auto",
  apply_weights = FALSE,
  apply_period_length = FALSE,
  asTibble = TRUE,
  as_data_table = FALSE,
  stringsAsFactors = FALSE,
  yearsAsFactors = FALSE,
  drop_duplicated_scenarios = TRUE,
  scenNameInList = as.logical(length(scen) - 1),
  unfold = TRUE,
  verbose = FALSE
)

\method{getData}{default}(scen, ...)

get_data(scen, ...)

\method{getData}{technology}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{commodity}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{storage}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{supply}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{demand}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{trade}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{import}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{export}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{weather}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{model}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)

\method{getData}{repository}(
  obj,
  name = NULL,
  ...,
  merge = FALSE,
  interpolate = FALSE,
  years = NULL,
  process = FALSE,
  asTibble = TRUE,
  ignore.case = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{scen}{Object scenario or list of scenarios.}

\item{...}{filters for various sets (setname = c(val1, val2) or setname_ = "matching pattern"), see details.}

\item{name}{character vector with names of parameters and/or variables.}

\item{merge}{if TRUE, the search results will be merged in one dataframe; the named list will be returned if FALSE. When TRUE, a data.frame (empty if nothing matched) is always returned, never NULL.}

\item{timeframe}{controls sub-annual time aggregation of results that carry a \code{slice} column. One of \code{"lowest"} (default, aggregate/sum flows up to the coarsest level, normally \code{ANNUAL}), \code{"highest"} (native/finest, as stored), \code{"all"} (return every timeframe level stacked), or an explicit calendar level name (e.g. \code{"SEASON"}, \code{"YDAY"}) to aggregate to that level. Non-slice data, and state/level variables (e.g. \code{vStorageStore}) for which summing over slices is meaningless, are returned unchanged.}

\item{process}{if TRUE, dimensions "tech", "stg", "trade", "imp", "expp", "dem", and "sup" will be renamed with "process".}

\item{parameters}{if TRUE, parameters will be included in the search and returned if found.}

\item{variables}{if TRUE, variables will be included in the search and returned if found.}

\item{maps}{if TRUE, map-type parameters (membership mappings, no \code{value} column) are also returned.}

\item{ignore.case}{grepl parameter if regular expressions are used in '...' or 'name_'.}

\item{newNames}{renaming sets, named character vector or list with new names as values, and old names as names - the input parameter to renameSets function. The operation is performed before merging the data (merge parameter).}

\item{newValues}{revalue sets, named character vector or list with new values as values, and old values as names - the input parameter to revalueSets function. The operation is performed after merging the data (merge parameter).}

\item{na.rm}{if TRUE, NA values will be dropped.}

\item{digits}{if integer, indicates the number of decimal places for rounding, if NULL - no actions.}

\item{drop.zeros}{logical, should rows containing zero values be filtered out.}

\item{asTibble}{logical, if the data.frames should be converted into tibbles.}

\item{stringsAsFactors}{logical, should the sets values be converted to factors?}

\item{yearsAsFactors}{logical, should \code{year} be converted to factors? Set 'year' is integer by default.}

\item{scenNameInList}{logical, should the name of the scenarios be used if not provided in the list with several scenarios?}

\item{verbose}{logical, print progress and diagnostic messages.}

\item{interpolate}{if TRUE, expand year-bearing object slots over the target
years using each parameter's default interpolation rule (for quick
demonstration plots). Default FALSE (raw data as stored). Only meaningful
for object/model/repository methods.}

\item{years}{integer vector of target milestone years for \code{interpolate}
(default: the yearly range observed in the data).}
}
\description{
Generic accessor. Dispatches on the object class:
\itemize{
\item \code{scenario} (or a list of scenarios): interpolated/solved data
from \code{modInp}/\code{modOut} -- see the \code{scenario} method below.
\item model objects (\code{technology}, \code{commodity}, \code{storage},
\code{supply}, \code{demand}, \code{trade}, ...): the object's own
\emph{raw input} slot data (pre-interpolation).
\item \code{model} / \code{repository}: raw input slots stacked across all
contained objects.
}

Model-object methods (\code{technology}, \code{commodity}, \code{storage},
\code{supply}, \code{demand}, \code{trade}) return the object's raw input
slot data. \code{model}/\code{repository} stack that across all contained
objects. Use \code{name} to select slot(s) (e.g. \code{"invcost"}), \code{...}
to filter (\code{region=}, \code{year=}, \code{comm=}, or \code{col_=}regex),
\code{merge=TRUE} for one long tidy frame (\code{param}/\code{value}), and
\code{interpolate=TRUE} to expand year-bearing slots over \code{years} (or the
observed year range) with each parameter's default interpolation rule.
}
\examples{
\dontrun{
data("utopia_scen_BAU.RData")
getData(scen, name = "pDemand", year = 2015, merge = TRUE)
getData(scen, name = "vTechOut", comm = "ELC", merge = TRUE, year = 2015)
elc2050 <- getData(scen, parameters = FALSE, comm = "ELC", year = 2050)
names(elc2050)
elc2050$vBalance
}
}
\seealso{
the per-class methods for the full argument list.
}
