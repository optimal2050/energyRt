% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utopia-geoscale.R
\name{utopia_geoscale}
\alias{utopia_geoscale}
\title{A geoscale for the UTOPIA reference model}
\usage{
utopia_geoscale(layout = "honeycomb", region = NULL, area = TRUE)
}
\arguments{
\item{layout}{Which layout in \code{utopia$map} to take geometry from:
\code{"honeycomb"} (default, the one the vignettes draw), \code{"squares"},
\code{"island"} or \code{"continent"}. \code{NULL} builds the hierarchy with no geometry,
which needs neither \code{sf} nor a map.}

\item{region}{Optional subset of regions to keep, e.g. \code{c("R1","R2","R3")}
to match the three-region UTOPIA model.}

\item{area}{Add an \code{area} weight measured from the geometry. The layouts
carry no CRS, so this is planar area in the coordinates' own units.}
}
\value{
A \code{geoscales::Geoscale}.
}
\description{
Builds a \link[geoscales:Geoscale]{geoscales::Geoscale} over UTOPIA's eleven regions, nested
\code{nation -> zone -> region}, and attaches one of the reference map layouts.
}
\details{
The hierarchy comes from \code{utopia$geo} and is keyed by region name, so it is
valid for every layout in \code{utopia$map} — the layouts place \code{R1}…\code{R11}
differently but share their names.
}
\examples{
\dontrun{
gs <- utopia_geoscale()
geoscales::geoscale_children(gs, "zone", "WEST")

# the three-region model used in vignette("utopia-build")
gs3 <- utopia_geoscale(region = c("R1", "R2", "R3"))
mod <- newModel("UTOPIA", region = c("R1", "R2", "R3"), geoscale = gs3)
}

}
\seealso{
Other geoscale:
\code{\link[=check_geoscale_regions]{check_geoscale_regions()}},
\code{\link[=plot_map]{plot_map()}},
\code{\link{setGeoscale,config-method}}
}
\concept{geoscale}
