% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-modelData.R
\docType{class}
\name{class-modelData}
\alias{class-modelData}
\title{A virtual S4 class for the model's data-carrying objects}
\description{
\code{modelData} is the common parent of \link{class-parameter} (the model's input
data) and \link{class-variable} (its solved output). It declares no slots and
cannot be instantiated: it exists so the storage contract those two share --
a \code{data} slot that may be materialised in memory or read lazily from an
Arrow dataset, plus a \code{misc} list holding \code{path} / \code{inMemory} / \code{onDisk} --
is implemented once rather than duplicated per class.

Methods dispatching on it: \code{print}, \code{.reset_data}. The rest of the contract
(\code{get_data_slot}, \code{isOnDisk}, \code{getObjPath}, \code{setObjPath}, \code{mark_ondisk},
\code{mark_inMemory}) is already written generically over "any S4 object with a
\code{misc} list" and needs no dispatch at all.

Deliberately slot-free. The two classes' similar-looking slots do not mean
the same thing -- \code{parameter@dimSets} is validated against \code{.dimSets} and
drives its \code{data} skeleton positionally, while \code{variable@dimSets} carries
duplicate dimensions that a positional skeleton cannot express -- so hoisting
them would buy nothing and prevent the two from diverging. It also keeps the
change backward compatible: an S4 object serialises as an attribute list
keyed by slot NAME, so a parent that adds no slot names loads cleanly from a
scenario saved before this class existed, which \code{load_scenario()} relies on
(it uses a plain \code{load()}, with no \code{updateObject()} step).
}
\keyword{internal}
