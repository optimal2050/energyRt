% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_object.R
\name{getObject}
\alias{getObject}
\alias{getObject.default}
\alias{getObject.repository}
\alias{getObject.model}
\alias{getObject.scenario}
\title{Retrieve model objects from a repository, model or scenario}
\usage{
getObject(x, ...)

\method{getObject}{default}(x, ...)

\method{getObject}{repository}(
  x,
  class = NULL,
  name = NULL,
  desc = NULL,
  region = NULL,
  ...,
  regex = FALSE,
  ignore.case = TRUE,
  region_agnostic = TRUE,
  drop = FALSE
)

\method{getObject}{model}(
  x,
  class = NULL,
  name = NULL,
  desc = NULL,
  region = NULL,
  ...,
  regex = FALSE,
  ignore.case = TRUE,
  region_agnostic = TRUE,
  drop = FALSE
)

\method{getObject}{scenario}(
  x,
  class = NULL,
  name = NULL,
  desc = NULL,
  region = NULL,
  ...,
  regex = FALSE,
  ignore.case = TRUE,
  region_agnostic = TRUE,
  drop = FALSE
)
}
\arguments{
\item{x}{a \code{repository}, \code{model} or \code{scenario}.}

\item{...}{additional per-slot filters forwarded to the matching engine, e.g.
\code{timeframe = "HOUR"}. Character filters are exact by default; a data.frame
slot is filtered by named columns. Filtering by a slot implicitly restricts
results to classes that have it.}

\item{class}{character vector of object classes to keep (e.g. \code{"technology"},
\code{c("supply", "import")}); \code{NULL} (default) keeps all classes.}

\item{name}{character, object name(s) to match against \verb{@name}.}

\item{desc}{character, description pattern(s) to match against \verb{@desc}.}

\item{region}{character, keep objects belonging to any of these regions.
Region-agnostic objects (e.g. commodities, which carry no region) are kept
for every region unless \code{region_agnostic = FALSE}.}

\item{regex}{logical; treat \code{name}, \code{desc} and character \code{...} filters as
regular expressions instead of exact matches (default \code{FALSE}).}

\item{ignore.case}{logical; case-insensitive matching for \code{regex = TRUE}
(default \code{TRUE}).}

\item{region_agnostic}{logical; whether objects with no region information
satisfy a \code{region} filter (default \code{TRUE}).}

\item{drop}{logical; if \code{TRUE} and exactly one object matches, return that
object itself instead of a one-element list (default \code{FALSE}).}
}
\value{
A named list of model objects keyed by \verb{@name} (empty list if none
match); or a single object when \code{drop = TRUE} and exactly one matches.
}
\description{
\code{getObject()} returns the model building-block objects held in a container --
commodities, technologies, supplies, storages, and so on -- selected by
\strong{class}, \strong{name}, \strong{description}, \strong{region} and/or any object \strong{slot}.
It is the object-level counterpart of \code{\link[=getData]{getData()}}, which returns those
objects' parameter data.
}
\details{
Class, name, description and any other slot are matched by the same engine
that powers the internal object accessors; the \strong{region} filter is applied
with \code{\link[=get_region]{get_region()}}, which reads regions uniformly from \verb{@region} slots and
from the \code{region}/\code{src}/\code{dst} columns of data.frame slots, so it works for
every class (including \code{import}/\code{export}/\code{trade}, which store region
structurally). A scenario is unwrapped to its model automatically.
}
\examples{
\dontrun{
repo <- utopia_modules$electricity$reg3$repo
getObject(repo, class = "technology")                 # all technologies
getObject(repo, class = c("supply", "commodity"))     # two classes
getObject(repo, region = "R1")                         # everything in R1
getObject(repo, name = "ECOA", drop = TRUE)            # the ECOA object
getObject(repo, desc = "coal", regex = TRUE)           # by description
getObject(repo, class = "technology", timeframe = "HOUR")  # slot filter
}
}
\seealso{
\code{\link[=getData]{getData()}}, \code{\link[=get_region]{get_region()}}, \code{\link[=find_in_model]{find_in_model()}}
}
