% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/levcost_storage.R
\name{levcost_storage_}
\alias{levcost_storage_}
\title{Levelized cost of storage}
\usage{
levcost_storage_(
  object,
  comm = NULL,
  cycles = 365,
  price = NULL,
  repo = NULL,
  fuel_costs = NULL,
  discount = 0.05,
  base_year = NULL,
  horizon = NULL,
  region = NULL,
  solver = solver_options$glpk,
  method = c("auto", "analytic", "solve"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{storage} object.}

\item{comm}{Commodity to price the output in. Defaults to the discharged
commodity.}

\item{cycles}{Full charge/discharge cycles per year. A store's levelized cost
is inversely proportional to how hard it is worked, and no slot carries
that, so it is an argument. \code{365} is one cycle a day.}

\item{price}{Price of the charging commodity, per unit. Taken from
\code{fuel_costs}, then from a matching \code{supply} in \code{repo}, then \code{0}.}

\item{repo, fuel_costs, discount, base_year, horizon, region, solver, method, verbose}{As in \code{\link[=levcost]{levcost()}} for a technology.}

\item{...}{Ignored, for signature compatibility with the technology method.}
}
\value{
A \code{levcost} object -- the same shape the technology method returns.
}
\description{
Called through \code{\link[=levcost]{levcost()}} on a \code{storage} object. See
\code{vignette("levcost")} for the metric; the header of this file for the algebra.
}
