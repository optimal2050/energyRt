% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_region.R
\name{get_region}
\alias{get_region}
\title{Collect the regions an object operates in}
\usage{
get_region(obj)
}
\arguments{
\item{obj}{a model object (S4) such as \code{technology}, \code{storage}, \code{trade},
\code{import}, or \code{export}.}
}
\value{
a character vector of region labels (possibly empty).
}
\description{
Generic, reflective accessor that walks every slot of an S4 model object
(except \code{misc}) and gathers the regions it refers to. Regions are read from:
\itemize{
\item any atomic slot named \code{region}, \code{src}, or \code{dst}, and
\item the \code{region}, \code{src}, and \code{dst} columns of any \code{data.frame} slot.
}
}
\details{
The result is the set of unique, non-missing, non-empty region labels. The
function is intentionally schema-agnostic so it keeps working as region
information is added to classes that do not yet carry an explicit \verb{@region}
slot (e.g. \code{import} / \code{export}).
}
\seealso{
Other model:
\code{\link[=find_in_model]{find_in_model()}}
}
\concept{model}
