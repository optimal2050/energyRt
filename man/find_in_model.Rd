% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{find_in_model}
\alias{find_in_model}
\title{Find where value(s) are stored across a model's objects}
\usage{
find_in_model(x, pattern, fixed = TRUE, slots = NULL, classes = NULL)
}
\arguments{
\item{x}{a \code{model}, \code{scenario}, \code{repository}, or a single model object (S4).}

\item{pattern}{character vector of value(s) to look for.}

\item{fixed}{if \code{TRUE} (default) match exactly; if \code{FALSE}, treat \code{pattern}
as a regular expression (matched with \code{\link[=grepl]{grepl()}}, alternated over the vector).}

\item{slots}{optional character vector restricting which slot names to search.}

\item{classes}{optional character vector restricting which object classes to
search (e.g. \code{"technology"}).}
}
\value{
a \code{data.frame} with columns \code{object}, \code{class}, \code{slot}, \code{column},
\code{value}, \code{n} -- one row per (object, slot, column, matched value); \code{column}
is \code{NA} for atomic slots and \code{n} counts the matching elements/rows. Empty
(0-row) data.frame when nothing matches.
}
\description{
Reflectively walks every slot of every object in a \code{model} / \code{scenario} /
\code{repository} (or a single S4 model object) and reports where the given
value(s) appear: the object, its class, the slot, and -- for \code{data.frame}
slots -- the column. Handy for tracking down a stray label, e.g. an
undeclared region (\code{"ES_off"}) or a mistyped commodity.
}
\examples{
\dontrun{
find_in_model(mod, c("ES_off", "PT_off"))   # locate stray regions
find_in_model(scen, "BIO", fixed = FALSE)    # regex over every object
find_in_model(mod, "ES_off", classes = "technology")
}
}
\seealso{
Other model:
\code{\link[=get_region]{get_region()}}
}
\concept{model}
