% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utopia_profiles.R
\name{utopia_profiles}
\alias{utopia_profiles}
\title{UTOPIA input profiles (deterministic)}
\usage{
utopia_profiles(
  regions,
  calendar = c("utopia_s4h24", "utopia_m12h24", "utopia_seasons"),
  source = c("saved", "ideea"),
  resources = c(WSOL = "WSOL", WWIN = "WWIN", WHYD = "WHYD"),
  cluster = 1L,
  diversify = TRUE
)
}
\arguments{
\item{regions}{character vector of region names.}

\item{calendar}{target resolution: \code{"utopia_s4h24"} (4 seasons x 24 hours, 96
slices, the default base case), \code{"utopia_m12h24"} (12 months x 24 hours,
288) or \code{"utopia_seasons"} (4 seasons x 3 dayparts, 12).}

\item{source}{\code{"saved"} (packaged data, default) or \code{"ideea"} (re-aggregate
from \code{IDEEA::ideea_modules} if installed).}

\item{resources}{named character vector mapping resource keys (\code{WSOL},
\code{WWIN}, \code{WHYD}) to IDEEA element names, used when \code{source = "ideea"}.}

\item{cluster}{integer, which IDEEA resource cluster to use (\code{source = "ideea"}).}

\item{diversify}{logical (default \code{TRUE}): scale the solar and wind capacity
factors by deterministic per-region factors (defined for the UTOPIA map
regions \code{R1}--\code{R11}; other names get factor 1), so regions have different
renewable endowments -- sunnier south, windier coast. \code{FALSE} replicates
identical profiles to every region.}
}
\value{
a list of tidy data.frames, each replicated across \code{regions}:
\code{weather} (\code{resource}, \code{region}, \code{slice}, \code{wval}), \code{demand} (\code{region},
\code{slice}, \code{load} -- a relative load shape) and \code{stock} (\code{region}, \code{tech},
\code{gw} -- base-year capacity).
}
\description{
Expand the saved, region-agnostic UTOPIA profiles (\link{utopia_weather},
\link{utopia_demand}, \link{utopia_stock}) to a set of regions for a chosen calendar.
Replaces the vignette's former random generators. The weather capacity
factors can be re-sourced at run time from IDEEA (\code{source = "ideea"});
\code{"saved"} (default) uses the packaged data and never needs an external
dataset.
}
\seealso{
\link{utopia_weather}, \link{utopia_demand}, \link{utopia_stock}, \link{calendars}
}
