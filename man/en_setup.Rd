% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{en_setup}
\alias{en_setup}
\title{Set up energyRt after installation}
\usage{
en_setup(ref = "optimal2050/energyRt")
}
\arguments{
\item{ref}{character. Package reference used to query system requirements on
Linux (default \code{"optimal2050/energyRt"}).}
}
\value{
A list, invisibly, with elements \code{os} (character), \code{deps} (the tibble
from \code{\link[=en_check_dependencies]{en_check_dependencies()}}) and \code{packages} (the tibble from
\code{\link[=en_check_packages]{en_check_packages()}}).
}
\description{
A one-call "am I ready?" entry point to run \emph{after} energyRt is installed. It
reports the operating system, prints the system libraries to install on Linux
(report only --- it never runs \code{sudo}), and prints the status tables from
\code{\link[=en_check_dependencies]{en_check_dependencies()}} (solver backends) and \code{\link[=en_check_packages]{en_check_packages()}} (R
packages, training extras, and external tools). It installs nothing itself:
use the
\code{install_energyRt()} bootstrap (see the Installation article) to install
energyRt and its dependencies, and \code{\link[=en_install_deps]{en_install_deps()}} to set up the
solver-backend library layer.
}
\examples{
\dontrun{
en_setup()
}
}
\seealso{
Other solver:
\code{\link{en_check}},
\code{\link[=en_check_packages]{en_check_packages()}},
\code{\link[=en_install_deps]{en_install_deps()}},
\code{\link[=en_install_python_deps]{en_install_python_deps()}},
\code{\link[=get_neos_email]{get_neos_email()}},
\code{\link{neos}},
\code{\link[=neos_build_gams_text_job]{neos_build_gams_text_job()}},
\code{\link[=neos_build_gams_xml]{neos_build_gams_xml()}},
\code{\link[=neos_gams_inline]{neos_gams_inline()}},
\code{\link{neos_job}}
}
\concept{solver}
