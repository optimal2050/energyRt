% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage_duration.R
\name{storage_duration}
\alias{storage_duration}
\title{Split a storage level into duration bands}
\usage{
storage_duration(
  scen,
  width = c(12L, 24L, 24L * 7L, 24L * 30L),
  labels = NULL,
  cyclic = NA,
  tmz = "UTC",
  stg = NULL
)
}
\arguments{
\item{scen}{a solved scenario.}

\item{width}{integer vector of window widths in HOURS, strictly increasing.
The default splits at half a day, a day, a week and 30 days, producing five
bands. Widths at or above the length of the series collapse to the series
minimum, which is the honest answer rather than an error.}

\item{labels}{optional character vector naming the bands, from shortest to
longest. Must be \code{length(width) + 1}. Defaults to labels derived from
\code{width}, e.g. \code{"<12h"}, \code{"12h-1d"}, \code{"1d-1w"}, \code{"1w-30d"}, \code{">30d"}.}

\item{cyclic}{how to pad the ends of the series before the rolling windows.
\code{NA} (default) takes it from each storage's \verb{@fullYear}: a store whose cycle
closes over the year genuinely continues into itself, so the head and tail
are wrapped. \code{FALSE} repeats the edge values instead. \code{TRUE}/\code{FALSE} force
one choice for every storage.}

\item{tmz}{time zone used to build the datetime column. \code{"UTC"} by default;
the original IDEEA script hard-coded \code{"Asia/Kolkata"}, which silently moved
every model's clock.}

\item{stg}{optional character vector restricting the storages considered.}
}
\value{
A data frame with one row per storage, commodity, region, year,
timeslice and band: columns \code{stg}, \code{comm}, \code{region}, \code{year}, \code{timeslice},
\code{datetime}, \code{duration} (an ordered factor, shortest first) and \code{value}.
Zero rows if the scenario has no storage level to decompose.
}
\description{
Decomposes \code{vStorageLevel} into how long the energy actually sits in the
store: the part cycling within a few hours, within a day, across a week, and
the part held for a month or more. The bands partition the level exactly --
they sum back to it at every timeslice -- so they can be stacked in an area
chart directly.
}
\section{Reading the result}{

A store cycling within its parent timeframe (\verb{@fullYear = FALSE}) cannot hold
energy for longer than that cycle, so its long-duration bands come out at or
near zero. That is a property of the model, not a failure of the split.
}

\examples{
\dontrun{
d <- storage_duration(scen)
library(ggplot2)
ggplot(d, aes(datetime, value, fill = duration)) +
  geom_area() +
  facet_wrap(~region)
}
}
