% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolation.R
\name{interpolate_model}
\alias{interpolate_model}
\title{Interpolate a model into a solver-ready scenario}
\usage{
interpolate_model(
  mod,
  name = NULL,
  ...,
  desc = NULL,
  ondisk = FALSE,
  overwrite = FALSE,
  fold = FALSE,
  sparse = TRUE,
  prune = TRUE,
  validate = TRUE,
  code = NULL,
  kvl = FALSE,
  boundary_prices = NULL,
  .prefilter = FALSE,
  verbose = isVerbose()
)
}
\arguments{
\item{mod}{a \link{model} object, or a \link{scenario} (its \verb{@model} is re-interpolated).}

\item{name}{character scenario name. If \code{NULL}, a default \verb{scen_<model>} is
used (with a warning).}

\item{...}{additional energyRt objects folded into the model BEFORE the
pipeline runs: \code{settings}, \code{config}, \code{calendar}, \code{horizon}, a whole
\code{repository}, or individual model "bricks" (\code{technology}, \code{commodity},
\code{storage}, ...). This is how a scenario overrides or extends the model (e.g.
pass a sampled \code{calendar} to interpolate on a reduced time resolution).}

\item{desc}{character scenario description.}

\item{ondisk}{logical; store each parameter's data in the on-disk parameter
store rather than the in-memory \verb{@data} slot. \code{FALSE} (default) keeps data in
memory, which the solver writers read directly; \code{TRUE} suits very large
models (data is materialised back to memory at solve time).}

\item{overwrite}{logical; overwrite an existing on-disk scenario of the same
name.}

\item{fold}{logical or character; whole-column "fold" of trimmable dimensions
to NA wildcards to shrink the data. \code{TRUE} folds \code{region} + \code{timeslice}; \code{FALSE}
(default) folds nothing; a character vector selects dims among
\code{region}, \code{timeslice}, \code{year}, \code{comm}, \code{tech}, \code{stg}, \code{trade}. A folded scenario
is expanded to solver-ready form at solve time.}

\item{sparse}{logical; the storage knob. \code{TRUE} drops \code{value == defVal} rows
(and folds); \code{FALSE} materialises the default over each parameter's full
domain (and unfolds).}

\item{prune}{logical; drop interpolated rows that fall outside the
equation-domain maps (no effect on the solution, smaller data).}

\item{validate}{logical; run \code{\link[=validate_scenario_parameters]{validate_scenario_parameters()}} after
interpolation (schema, duplicate keys, map/parameter coverage, missing
governing constraints, calendar chronology).}

\item{code}{optional named list overriding solver source-code blocks
(\code{GLPK}, \code{GAMS}, \code{JuMP}, \code{PYOMOConcrete}, ...), each either a script-file
path or a character vector of lines. Lets a model-script version be supplied
at interpolation time without rebuilding \code{sysdata} (handy to A/B templates).}

\item{boundary_prices}{optional \code{data.frame} pricing import/export stubs
for trade routes dropped by a SPATIAL SAMPLE (a
\code{geoscales::filter_geoscale()} subset passed via \code{...}); see
\code{\link[=subset_model_regions]{subset_model_regions()}} for the columns. Ignored (with a warning)
when no sampled geoscale is supplied.}

\item{.prefilter}{EXPERIMENTAL, default \code{FALSE}. Restrict each model
object's data to the timeslices and regions the SCENARIO declares before
interpolating it, rather than interpolating everything and discarding the
excess afterwards.

On the sampled-calendar recipe -- a full-year model with a subset calendar
handed to this function -- the default order expands all 8,760 timeslices
in \code{ob2mi} and then keeps 96. The parameter filters that follow are cheap
(0.05s on a 5-node model); the cost is the interpolation they cannot undo.

Off by default because it is not obviously safe: anything deriving a
relation from the full declared grid rather than from the calendar would
see a narrower input. Compare objectives before relying on it.}

\item{verbose}{logical; print per-step progress. This also governs the
variant-expansion report -- how many process objects were expanded and how
many constraints were generated for them. The generated constraints
themselves are retrievable with \code{getObject(scen, class = "constraint")};
each carries a readable \code{desc} and, in \code{misc$.variant_source}, the object
it was derived from.}
}
\value{
an interpolated \link{scenario} object.
}
\description{
Builds an interpolated \link{scenario} from a \link{model} via the mapping pipeline:
collects sets from the model objects, builds the membership / calendar /
lifespan / value / constraint / cost mappings, extracts and interpolates the
numeric parameters over the milestone years, and (optionally) folds, prunes
and validates the result. The returned scenario is ready for \code{\link[=solve_model]{solve_model()}} /
\code{\link[=solve_scenario]{solve_scenario()}}.
}
\seealso{
\code{\link[=solve_model]{solve_model()}}, \code{\link[=solve_scenario]{solve_scenario()}}, the \code{interpolate} S4 method.

Other interpolation:
\code{\link[=subset_model_regions]{subset_model_regions()}},
\code{\link[=with_solver_log]{with_solver_log()}}
}
\concept{interpolation}
