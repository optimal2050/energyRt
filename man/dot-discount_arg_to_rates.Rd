% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discount_rates.R
\name{.discount_arg_to_rates}
\alias{.discount_arg_to_rates}
\title{Translate a \code{discount} argument into a table of \code{wacc}/\code{sdr} rates}
\usage{
.discount_arg_to_rates(x)
}
\arguments{
\item{x}{a number, or a data.frame with \code{region}/\code{year} and either \code{discount}
or both \code{wacc} and \code{sdr}.}
}
\value{
a data.frame with \code{wacc} and \code{sdr} populated on every row.
}
\description{
\code{discount} is an argument, not a slot column. A bare number (or a table with
a \code{discount} column) means "one rate for both jobs" and is expanded into
equal \code{wacc} and \code{sdr}; a table may instead give \code{wacc} and \code{sdr} explicitly,
by region and year. Mixing the two forms in one row, or giving only one half
of the pair, is an error.
}
\keyword{internal}
