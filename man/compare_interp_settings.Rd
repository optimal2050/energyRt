% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_size.R
\name{compare_interp_settings}
\alias{compare_interp_settings}
\title{Compare interpolation settings (size & build time) for a model}
\usage{
compare_interp_settings(
  mod,
  settings = NULL,
  ...,
  name = "cmp",
  verbose = FALSE,
  top_n = 12L,
  keep_scen = FALSE
)
}
\arguments{
\item{mod}{a \code{model}.}

\item{settings}{a \emph{named} list of setting combinations; each element is a list
of arguments forwarded to \code{\link[=interpolate_model]{interpolate_model()}}, e.g.
\code{list(fold = TRUE, sparse = TRUE, prune = TRUE)}. When \code{NULL} a default grid
(dense / sparse / sparse+prune / fold+sparse+prune / fold-all) is used.}

\item{...}{arguments forwarded to EVERY \code{interp_mod()} call, e.g.
\code{horizon = newHorizon(period = 2024)}.}

\item{name}{base scenario name; each build is \code{"<name>_<setting>"}.}

\item{verbose}{forwarded to \code{\link[=interpolate_model]{interpolate_model()}} (per-build progress).}

\item{top_n}{number of largest parameters to keep per build.}

\item{keep_scen}{if \code{TRUE}, the interpolated scenarios are returned in
\verb{$scen} (handy to feed a later solve step). Off by default to save memory.}
}
\value{
an object of class \code{interp_settings_cmp}: a list with
\itemize{
\item \code{summary} — one row per setting (ok, seconds, mb, param_rows, counts,
variable/constraint estimates, error),
\item \code{top} — wide data.frame of the largest parameters' row counts per setting,
\item \code{details} — named list of per-build \code{\link[=model_size]{model_size()}} objects,
\item \code{scen} — named list of scenarios when \code{keep_scen = TRUE},
\item \code{settings} — the settings grid used.
}
}
\description{
Interpolates \code{mod} in memory under each of several setting combinations and
tabulates how big the resulting model is: total value-parameter rows, the
parameter / map / set counts, the variable & constraint estimate (from
\code{\link[=model_size]{model_size()}}), the in-memory size, and the build time. A quick way to see
how \code{fold} / \code{sparse} / \code{prune} trade off model size and speed before
committing to a solve.
}
\details{
All builds use \code{ondisk = FALSE} (so sizes are measured in memory and are
directly comparable) and \code{overwrite = TRUE}.
}
\examples{
\dontrun{
cmp <- compare_interp_settings(mod, horizon = newHorizon(period = 2024))
cmp                       # prints the comparison table
# custom grid:
compare_interp_settings(mod,
  settings = list(
    none    = list(fold = FALSE, sparse = FALSE, prune = FALSE),
    all     = list(fold = c("region","slice","year","comm","tech","stg","trade"),
                   sparse = TRUE, prune = TRUE)),
  horizon = newHorizon(period = 2024))
}
}
\seealso{
\code{\link[=model_size]{model_size()}}, \code{\link[=interpolate_model]{interpolate_model()}}
}
