% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw.R
\name{ghost_options}
\alias{ghost_options}
\title{Geometry of the faded "ghost" vintages behind a drawn technology}
\usage{
ghost_options(
  alpha = 0.45,
  alpha_min = 0.12,
  scale = 0.72,
  drift = 0.17,
  rise = 0,
  stamp = TRUE
)
}
\arguments{
\item{alpha}{opacity of the nearest ghost; further ones fade towards
\code{alpha_min}.}

\item{alpha_min}{opacity of the furthest ghost.}

\item{scale, drift, rise}{geometry of the ghost stack, all describing the
FURTHEST ghost rather than a per-step increment: its size relative to the
selected figure, and how far it is offset horizontally and vertically (in
npc). Intermediate ghosts interpolate. Defining the total extent this way
keeps a technology with thirty annual vintages inside the viewport -- a
per-step offset would put it off-canvas. \code{rise} defaults to 0: with ghost
arrows suppressed there is nothing for a vertical offset to clear, so the
stack reads better as a flat fan.}

\item{stamp}{label each ghost with its vintage. The first, last and selected
vintages are always stamped; intermediate ones only when the spacing
leaves room, so a dense stack does not turn into overlapping text.}
}
\value{
a list of class \code{ghost_options}.
}
\description{
Collects the ghost-stack settings of \code{\link[=draw]{draw()}} into one object, so the
drawing methods keep a short signature. Pass either this constructor or a
plain named list: \code{draw(x, ghost = list(drift = 0.2))} is merged onto the
defaults. Unknown names are an error in both forms -- misspelling a flat
argument used to be swallowed silently by \code{...} and forwarded downstream.
}
\examples{
ghost_options(drift = 0.25, stamp = FALSE)
}
\seealso{
Other draw:
\code{\link[=draw]{draw()}}
}
\concept{draw}
