% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_file.R
\name{en_config_path}
\alias{en_config_path}
\alias{en_config_read}
\alias{en_config_write}
\title{The energyRt configuration file}
\usage{
en_config_path(global = TRUE)

en_config_read(global = NULL)

en_config_write(config = NULL, global = TRUE, backup = TRUE)
}
\arguments{
\item{global}{logical. \code{TRUE} writes the user-wide config (see
\code{\link[=en_config_path]{en_config_path()}}), \code{FALSE} writes \code{./.energyRt.yml}.}

\item{config}{named list of option values, using names \emph{without} the \code{en.}
prefix (e.g. \code{list(gams_path = "C:/GAMS/48")}). \code{NULL} (default) collects
the currently non-default options.}

\item{backup}{logical. When writing the user-wide config, move any pre-0.74
files in the home directory (\verb{~/.energyRt/config.yml} and the deprecated
\verb{~/.energyRt.R}) aside to \verb{*.bak}. They would otherwise keep overriding the
new config. Set \code{FALSE} to leave them in place.}
}
\value{
character, the path. The file need not exist.

a named list of option values, possibly empty.

the written config, invisibly.
}
\description{
Solver paths and other options can be persisted to a YAML file so they are
restored in every session: the user-wide file under
\code{\link[tools:R_user_dir]{tools::R_user_dir()}}\verb{("energyRt", "config")}, or \code{./.energyRt.yml} for a
single project. energyRt applies them when the package loads, without
overriding anything the user has already set through an R option or an
environment variable.

Use \code{\link[=en_config_show]{en_config_show()}} to see which options are in effect and where each
value came from.

\code{en_config_read()} reads the project config (\code{./.energyRt.yml}) merged over
the user-wide one. It reports what is \emph{on disk}; use \code{\link[=en_config_show]{en_config_show()}} for
what is actually in effect.

\code{en_config_write()} persists options so they are restored in later sessions.
Called with no arguments it writes every option whose current value differs
from the package default -- so the usual workflow is to set paths
interactively, check them with \code{\link[=en_config_show]{en_config_show()}}, then call
\code{en_config_write()} once.
}
\section{Location}{

Up to energyRt 0.74 the user-wide file was \verb{~/.energyRt/config.yml}. CRAN
policy does not allow a package to write in the user's home filespace, so it
now lives in the standard R user configuration directory instead. The old
file is still \emph{read} (nothing breaks), and \code{\link[=en_config_write]{en_config_write()}} moves it aside
once the new one exists.
}

\examples{
en_config_path()
\dontrun{
set_solver_path("gams", "C:/GAMS/win64/48.1")
en_config_write()
}
}
\seealso{
Other options:
\code{\link[=en_config_show]{en_config_show()}},
\code{\link[=get_arrow_format]{get_arrow_format()}},
\code{\link[=get_default_solver]{get_default_solver()}},
\code{\link[=isVerbose]{isVerbose()}},
\code{\link[=set_option]{set_option()}},
\code{\link[=set_registry_file]{set_registry_file()}},
\code{\link[=set_scenarios_path]{set_scenarios_path()}},
\code{\link[=set_solver_path]{set_solver_path()}}
}
\concept{options}
