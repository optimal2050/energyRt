% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy_api_shims.R, R/solve.R
\name{solve_mod}
\alias{solve_mod}
\alias{solve_scen}
\alias{solve_model}
\alias{solve_scenario}
\title{Solve a model or an interpolated scenario}
\usage{
solve_mod(obj, ...)

solve_scen(obj, ...)

solve_model(obj, name = NULL, solver = NULL, ondisk = FALSE, fold = FALSE, ...)

solve_scenario(
  obj,
  name = obj@name,
  solver = NULL,
  solver.dir = NULL,
  transient = FALSE,
  force = FALSE,
  kvl = NULL,
  variant = NULL,
  ...,
  tmp.dir = NULL,
  tmp.del = NULL
)
}
\arguments{
\item{obj}{a model object (\code{solve_model()}) or an interpolated scenario
object (\code{solve_scenario()}).}

\item{...}{for \code{solve_model()}, arguments are routed to
\code{\link[=interpolate_model]{interpolate_model()}} (settings / calendar / horizon / model data) or to
the solver run (\code{solver.dir}, \code{transient}, \code{force}, \code{read.solution},
\code{wait}, \code{echo}, \code{run}, \code{run.conflict}, \code{n.threads}, ...). For
\code{solve_scenario()}, arguments are passed to \code{.executeScenario()}. Set
\code{echo = FALSE} for a quiet run: it silences both the progress messages
and the solver's own console output.

\code{run} doubles as the run label: \code{TRUE}/\code{FALSE} keeps its historical
meaning (run the solver or only write the script), while a character
value names the run — the solve lands in \verb{<scenario>/runs/<run>/} (or
\verb{runs/<variant>/<run>/} for an own-problem variant) with a \code{run.yml}
provenance record (default label: the solver directory name, e.g.
\code{"glpk"}). \code{run.conflict} controls an existing recorded run under the
same label: \code{"overwrite"} (default), \code{"suffix"} (append \code{-2}, \code{-3},
...), \code{"error"}. See \code{\link[=scenario_runs]{scenario_runs()}} to list runs and
\code{\link[=read_solution]{read_solution()}} to switch between them.}

\item{name}{character name of the scenario to create / return.}

\item{solver}{a character or list with solver settings. When \code{NULL}, the
scenario's own solver settings or \code{get_default_solver()} are used.}

\item{ondisk, fold}{passed to \code{\link[=interpolate_model]{interpolate_model()}}. Defaults (\code{FALSE}/\code{FALSE}) keep
parameters in memory and unfolded, matching the shape the writers expect.}

\item{solver.dir}{character, an EXTERNAL solver working directory. When
set, the solve runs there verbatim and records no run; the default
(\code{NULL}) solves into the scenario's own run directory
\verb{runs/[<variant>/]<label>/solver/}.}

\item{transient}{logical, use a throwaway timestamp directory deleted
after the run (no run record).}

\item{force}{logical, re-solve a scenario already solved to optimal.}

\item{variant}{character (\code{solve_scenario()} only): declare the scenario's
interpolated problem an OWN-PROBLEM VARIANT of this name. Its solves
land in \verb{runs/<variant>/<solve>/}, and \code{\link[=save_scenario]{save_scenario()}} stores the
variant's problem (\code{modInp}, settings snapshot, \code{variant.yml}) under
\verb{runs/<variant>/} — once, shared by all its solves — leaving the
scenario-level (base) problem untouched. Switch between problems with
\code{\link[=read_solution]{read_solution()}} (\code{run = "<variant>/<solve>"} vs \code{run = "<solve>"}).}

\item{tmp.dir, tmp.del}{deprecated aliases of \code{solver.dir} / \code{transient}.}
}
\value{
a scenario object with the solution.
}
\description{
\code{solve_model()} interpolates a model with \code{\link[=interpolate_model]{interpolate_model()}} and solves
it (an already-interpolated scenario passed to it is solved as-is; an
un-interpolated one is interpolated first). \code{solve_scenario()} solves a
scenario that was already interpolated with \code{\link[=interpolate_model]{interpolate_model()}} and
errors otherwise. Both reuse the write / run / read framework
(\code{.executeScenario()}); all solver backends (GLPK/GMPL, GAMS, Pyomo, JuMP)
work unchanged. The transitional names \code{solve_mod()} / \code{solve_scen()} are
deprecated aliases.
}
\seealso{
\code{\link[=interpolate_model]{interpolate_model()}}, \code{\link[=read_solution]{read_solution()}}
}
