% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.R
\name{solve_mod}
\alias{solve_mod}
\alias{solve_scen}
\title{Solve a model or an interpolated scenario built with the new pipeline}
\usage{
solve_mod(obj, name = NULL, solver = NULL, ondisk = FALSE, fold = FALSE, ...)

solve_scen(
  obj,
  name = obj@name,
  solver = NULL,
  tmp.dir = NULL,
  tmp.del = FALSE,
  force = FALSE,
  ...
)
}
\arguments{
\item{obj}{a model object (\code{solve_mod()}) or an interpolated scenario object
(\code{solve_scen()}).}

\item{name}{character name of the scenario to create / return.}

\item{solver}{a character or list with solver settings. When \code{NULL}, the
scenario's own solver settings or \code{get_default_solver()} are used.}

\item{ondisk, fold}{passed to \code{\link[=interpolate_model]{interpolate_model()}}. Defaults (\code{FALSE}/\code{FALSE}) keep
parameters in memory and unfolded, matching the shape the writers expect.}

\item{...}{for \code{solve_mod()}, arguments are routed to \code{\link[=interpolate_model]{interpolate_model()}}
(settings / calendar / horizon / model data) or to the solver run
(\code{tmp.dir}, \code{tmp.del}, \code{force}, \code{read.solution}, \code{wait}, \code{echo}, \code{run},
\code{n.threads}, ...). For \code{solve_scen()}, arguments are passed to
\code{.executeScenario()}. Set \code{echo = FALSE} for a quiet run: it silences both
the progress messages and the solver's own console output.}

\item{tmp.dir}{character path to the solver working directory.}

\item{tmp.del}{logical, delete the working directory after the run.}

\item{force}{logical, re-solve a scenario already solved to optimal.}
}
\value{
a scenario object with the solution.
}
\description{
\code{solve_mod()} interpolates a model with \code{\link[=interpolate_model]{interpolate_model()}} and solves it.
\code{solve_scen()} solves a scenario that was already interpolated with
\code{\link[=interpolate_model]{interpolate_model()}}. Both reuse the existing write / run / read framework
(\code{.executeScenario()}), so any solver backend supported by the legacy
\code{\link[=solve_model]{solve_model()}} (GLPK/GMPL, GAMS, Pyomo, JuMP) works unchanged.
}
\seealso{
\code{\link[=solve_model]{solve_model()}}, \code{\link[=interpolate_model]{interpolate_model()}}, \code{\link[=read_solution]{read_solution()}}
}
