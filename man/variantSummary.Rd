% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variants.R
\name{variantSummary}
\alias{variantSummary}
\title{Summarise a result variable across technology variants}
\usage{
variantSummary(scen, name, by = character(), weight = NULL, ...)
}
\arguments{
\item{scen}{a solved \code{scenario}.}

\item{name}{character, name of the variable or parameter (e.g. \code{"vTechCap"}).}

\item{by}{character vector, variant dimensions to keep as grouping columns:
any of \code{"vintage"} and \code{"cluster"}. Empty (default) summarises across all
variants of a base technology.}

\item{weight}{optional character, name of a variable to use as weights for
the mean (e.g. \code{"vTechCap"} to capacity-weight a per-unit quantity). When
\code{NULL} the mean is unweighted.}

\item{...}{further filters passed to \code{\link[=getData]{getData()}} (e.g. \code{year = 2050}).}
}
\value{
A data.frame with the grouping columns plus \code{n}, \code{total}, \code{mean},
\code{sd}, \code{min} and \code{max}.
}
\description{
Aggregates a solved variable over the variants of each base technology and
reports the spread across them: total, mean, standard deviation, min and max,
plus the number of variants contributing. This is the "analyse by cluster and
vintage" view -- e.g. the spread of capacity factors across wind resource
clusters, or of capacity across vintages.
}
\seealso{
Other technology process:
\code{\link[=getVariants]{getVariants()}},
\code{\link[=newTechnology]{newTechnology()}},
\code{\link[=read_technology_xlsx]{read_technology_xlsx()}},
\code{\link[=write_technology_xlsx]{write_technology_xlsx()}}
}
\concept{technology process}
