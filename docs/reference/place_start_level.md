# Place `pStorageStartLevel` on the first timeslice of each cycle

Place `pStorageStartLevel` on the first timeslice of each cycle

## Usage

``` r
place_start_level(scen)
```

## Arguments

- scen:

  scenario after interpolation

## Value

scenario with `pStorageStartLevel` rewritten
