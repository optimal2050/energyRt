# Search through model data for set elements

Search through model data for set elements

## Usage

``` r
collect_set_elements(obj, set_name)
```

## Arguments

- obj:

  model or another S4 object

- set_name:

  name of the set to search for

## Value

a character vector of set elements
