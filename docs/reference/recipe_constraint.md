# Build constraint mappings (recipe 7)

Build constraint mappings (recipe 7)

## Usage

``` r
recipe_constraint(scen, names, fmp)
```

## Arguments

- scen:

  scenario object.

- names:

  character vector of constraint mapping names to build.

- fmp:

  function mapping a parameter name to its on-disk path.

## Value

updated scenario object.
